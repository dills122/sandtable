# Creation-rooted released-I ordinal-2 Movement v1

Status: `CMB-TASK-003D2c.3l` contract checkpoint. The
[combined Combat plan](../design/combat-cycle-implementation-plan.md),
[guarded cycle control](combat-inherited-cycle-control-v1.md),
[ordinary Movement](combat-ordinary-movement-v1.md), and
[continual-cycle design](../design/continual-cycle-reserve-composition-v1.md) govern this packet.
Contract evidence only; no Movement completion, exception expiry, Breakdown, Combat, Snapshot12,
runtime, public action, Exercise or simulator activation.

## Objective and exact profile

Replay each canonical 3k `repeat` terminal at authority27 and ordinal2 Movement. Admit its sole
released-I member for one adjacent featureless-Clear move from assault location to own rear. Charge
2CP from cumulative0, no excess-CPA DP, and retain the released-I voluntary ceiling10, ammunition10,
TOE10, Cohesion, RNG, attack history and pending ordinal2 Movement exception.

This packet proves released-history Movement admission and one atomic move. It does not claim the
exception was required by current proximity: both exact cycle1 Movement-end proofs remain eligible.
It also does not create a completion receipt. `3m` must compose actual Movement stop/completion and
then apply the frozen D2b.2 expiry projection.

## Contract and authentication

`InheritedReserveMovementBase` carries actor, exact 3k predecessor hash/base/two trusted inputs/two
canonical events, active ordinal2 authority and Movement position, full World/RNG/attack history,
release member and prior Movement-end proof. Reader independently regenerates and byte-compares the
entire 3k trace; matching caller hashes never suffice.

`InheritedReserveMovementState` binds base hash, authority version/prefix, World/RNG/history,
release member, D2a route tracks and receipts. Accepted Movement updates both World CP and member
`spentCp` from0→2 while retaining all other release-history fields and the pending exception. Input
and event reuse D2a closed canonical shapes and receipt/prefix domains. Adapter changes only
prerequisites D2a deliberately deferred:
ammo10 is allowed because exact armed proof remains authenticated, and cost uses release=`I`, giving
ceiling10. Every other D2a atomic movement, occupancy, relationship, CP/DP, representation, retry,
capacity and byte rule remains unchanged.

## Success and rejection

Fixture freezes axis/commonwealth traces with one event each. Success requires authority27→28,
assault→own-rear, CP0→2, DP0, no ended relationships, one route and one receipt. Only element and
representation location plus World/member CP change. Release metadata and pending exception remain
byte-identical.

Reject finish predecessors, wrong actor/cycle/position/unit/origin/destination, nonadjacent or
occupied destinations, changed World/resource/history/proof, stale/duplicate-altered commands,
overflow, unsupported released type/ordinal/exception, malformed bytes and self-consistent source
rehashing. Exact retry returns original receipt without event or debit.

## Traceability and next gates

Advances CYCLE-COMP-AC-006/009/010 and CON-002–004 for one private released-I atomic move. `3m`
still owes accepted Movement completion and exact exception expiry; broader Reaction/vehicle/
Reserve profiles, D2c.4, Task004, checkpoint B and runtime/public/simulator work remain open.
