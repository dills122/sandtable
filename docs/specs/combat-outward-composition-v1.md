# Combat outward composition v1 — C

Accepted 2026-09-18: bounded contract integration index for Task004/checkpoint B. Private and unregistered;
no runtime payload, production admission, hosted Runner, Dispatch, provider or War Diary activation.
Acceptance closes the selected contract-model boundary only. Task005 dormant C# arithmetic/codecs
and the 72 planned runtime AC tests remain separately owned work.

## Exact requirement mapping

The schema retains all 72 original AC IDs, requirement text, design-source line, decisions,
planned tasks, planned test alias and runtime owners. Every row carries explicit bounded/partial/
deferred evidence coverage. The imported `contractEvidenceDraft` field name is preserved for
traceability; it names retained contract evidence, not a claim that planned runtime tests ran.
Readback rejects omitted, duplicated, reordered, unknown or modified rows, including altered
planned owners/tests. Each design source is independently SHA-pinned and each stated source line
must contain its exact AC ID. Named oracle aliases resolve to pinned repository files.

This index binds predecessor specs, ordered schemas, oracles and retained fixtures, including
historical versions. Canonical plan/navigation/planning files are not dependencies or hash seeds.
The original historical shared-clock counterexample remains historical diagnostic evidence;
current corrected profiles use accepted Round2, Result2 and native result-to-cycle bridge policies.
Historical Snapshot1 is not relabeled as a corrected full-session checkpoint. B1's typed checkpoint
is the current private composition boundary.

## Explicit source lanes

- A1 corrected selection/round and A2 corrected settlement use immutable codecs1/2.
- A3 live inherited release-I/control uses full-World records and its bounded retained-clock policy.
- A3 Reserve/cycle canonical ledgers use `LedgerObservation3` and remain outside full-World admission.
- A3b corrected Combat-through-finish uses codec3 and binds assignment/result/bridge policy and all
  budgets from its first public frame. Finish is the only offered bridge control action.
- A3b exact28 historical terminals are projection-only; zero execution does not invent earlier
  public actions or receipts. Original source/control lifecycle and singleton Reserve records remain.
- B1 authenticates all134 exact source references across its three execution scopes. B2 executes
  private child evidence; valid failure remains failure. B3 validates before aggregation/comparison.
  Unequal-stream null-arm handling remains algorithm-only evidence; current admitted comparable
  pairs have equal lengths. No new source family is introduced by integration.

The Task003 handoff retains exact 28 trace IDs, versions, composition digest and canonical handoff
digest. Accepted composition reader and B1/side retained handoffs must agree. Existing artifact
labels, historical schemas and original audience traces are not rewritten or newly registered.

## Readback and measured capacity

Selected witnesses regenerate outward views, a source and initial/terminal checkpoints, verified
and failed child evidence, and compared/unavailable parent reports with accepted readers. The
fixture stores witness hashes and byte lengths rather than duplicating predecessor payloads.
Exact source pins are checked before any cached integration value. Accepted reader caches remain
bound to authenticated immutable inputs; returned index records are copied.

Capacity rows measure actual retained canonical records with their own codecs, grouped by explicit
profile/type. Each row retains record count, maximum encoded bytes/depth/array size and declared
reader limits. A multi-record fixture file is not one payload. Standalone ledger capacity does not
expand full-World admission; old codec bounds and candidate domains remain separate. Selected
source-backed reader witnesses are distinguished from the broader retained-record codec survey.

Negative integration checks cover incorrect pins and mappings, guessed occurrence, forged child
success/proof, cross-version/profile proposals, changed source-bound parent evidence, malformed
index bytes and exact fixture mutations. Current source authentication precedes cache use; no
private source hash is inserted into outward identity seeds.

## Strict index contract

Ordered schema descriptors define Index, requirement/evidence mappings, aliases, artifacts,
profiles, handoff, capacities, reader witnesses and exclusions. Unknown/missing/duplicate/reordered
properties, non-ASCII wire bytes, trailing data, float/bool integer encodings and bounds violations reject.
Original Unicode requirement prose is preserved through canonical ASCII JSON escapes; its bounded
text fields reject control characters and unpaired surrogates.
Index limits: 2 MiB, depth56,512 array items; exactly72 requirement rows. Source codecs keep their
own limits. Retained fixture is exact ASCII JSON with two-space indentation and final newline.
Readback recomputes the complete index from immutable pinned artifacts and selected accepted readers.
