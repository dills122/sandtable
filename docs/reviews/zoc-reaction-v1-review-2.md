# ZOC and Reaction v1 Independent Review 2

**Review instance:** 2 of 3

**Date:** 2026-08-30

**Reviewed target:** remediated dirty working-tree documentation delta on
`codex/zoc-reaction-specification` from
`37e2f61c657d43b90dce28aa8d4396504a8963eb`

**Frozen-target verdict:** Not ready

**Post-reconciliation disposition:** Remediated; final review instance 3 required before approval

## Findings and reconciliation

| Finding | Severity | Disposition | Resolution |
| --- | --- | --- | --- |
| Topology-local Movement ZOC could not be derived from the defined side-safe Observation 6 | P1 | **Accept** | Proposed `ZOR-DEC-012` and `ZOR-REQ-009`/`011` now add one ordinal aggregate `apparentEnemyControlledLocationIds` set with no source mapping or rationale. Tasks 004A-006C own dormant projection, strict codec, local derivation, equivalence, and atomic publication; `ZOR-AC-015` freezes disclosure/fog behavior. |
| Frozen opportunities did not require each participant to be adjacent to the triggering mover | P1 | **Accept** | `ZOR-REQ-005` and the eligibility algorithm now restrict every participant to an authoritative represented non-phasing combat element individually adjacent to the committed destination, retain trigger-time adjacency evidence, and add multiple-adjacent/remote-negative `ZOR-AC-016` proof. |
| Task 002 claimed creation/World/replay seed evidence before Task 003 implemented those seams | P2 | **Accept** | Task 002 now closes only Rules/Content identity, provenance, admission, static vectors, and Content-only seed negatives. Full `ZOR-AC-014` and the creation/World/replay portions of requirements 002/012 move to Tasks 003A-003B. |

No finding was disputed or deferred.

## Residual-risk clarifications

The reviewer also asked whether a window can close during an active episode and how system close
reasons are authorized. The revised package makes player close unavailable while active. Exact
reason-specific System-audience actions may close an active window after at least one accepted step,
resolve active/remaining opportunities without reversing prior CP/BP, and reject free-form,
unknown, wrong-audience, or mismatched reasons. `ZOR-AC-017` and Task 006A own this evidence.

## Confirmed prior remediations

Instance 2 confirmed the Rule 8.53(b) declaration-aware deferral, first-step participant selection,
current-TOE authority design intent, combat-only trigger, Core-only fallback boundary, stale Movement
status correction, atomic publication concentration, and major replay/scope boundaries.

## Independent verification

- Confirmed exact branch/base, no staged changes, eight modified and three untracked delivery files.
- Inspected all delivery files, prior review, official source pages, canonical research, current
  Movement/observation/action/state seams, and version inventory.
- `git diff --check` passed for tracked changes; untracked files had no whitespace diagnostics.
- Selected local Markdown target check passed.
- The reviewer did not rerun the full gate; the implementation task's remediated gate recorded 995
  passed, zero failed/skipped, and a warning-clean build.

## Next review

The Observation disclosure contract and participant-universe changes are material fog/source
revisions. Review instance 3 of 3 must verify the complete final target. Any remaining finding after
that pass returns to the owner without another automatic review.
