# Task018C author explanation

## Intent and scope

Admit one real released-I ordinal2 Clear move after authenticated019C repeat, matching frozen3l
for both actual owners. World/member CP advance together 0→2 under ceiling10; locations advance
assault→own rear; pending exception persists. Same branch as019C, base4f1b51c. Five-primary-file
manifest matches dispatch: new engine/models, codec, tests, test fixture link and canonical plan.
Administrative navigation/evidence separate; no old APIs or frozen contracts changed.

## Flow and component responsibilities

`CombatInheritedReserveMovementSource` owns copies of two independent control inputs/events.
Derive replays019C, including creation/held-I Combat/Release/armed proof, before comparing exact
frozen3k repeat terminal hash. Finish and other valid timings are outside this profile. It binds
ordinal2, authority27, member status/spending, released-I rights and pending scope, then constructs
actual-side Movement position. Terminal pins supplement full replay; hashes alone never admit.

Engine replays at most one separately admitted Movement input/event. Apply replays first, compares
optional cache and checks actor/unit/cycle/position before retry lookup. Exact retry returns original
bytes and unchanged state. A second distinct move rejects even if otherwise legal, preserving this
bounded child. Transition checks current origin, real content adjacency, featureless Clear terrain,
independent infantry, resources, ledger scope and occupancy. Shared018A rules charge the released-I
ceiling. New element/representation location and World/member CP are built as one immutable result.

Codec composes canonical019C base/state and existing Movement command/input encoding. Frozen event,
receipt and prefix semantics match D2a. Source-bound base/state readers rederive and compare all
bytes. Shared Movement bounds enforce ASCII canonical records, duplicate keys, depth32, array512
and1MiB capacity. Parsed JSON never supplies authority.

## Choices and alternatives

Existing `ProjectOrdinaryMove` intentionally requires settled, exhausted-ammunition history. Widening
it would mix independent admissions. Instead, this exact no-settlement source uses shared cost rules
and existing validating World7 constructor. Source guarantees no relationships/DP/settlement work;
explicit checks retain that limit. No generic World validation or isolated Movement API is loosened.
Small specialized event projection preserves existing canonical shape without a broad refactor.

## Invariants and verification

Only selected element/representation location and World/member CP may differ. Ammunition10, TOE10,
Cohesion, other element, all World ledgers, RNG, attack history and complete Release history remain.
Exception stays pending; this move does not prove the exception was necessary for current proximity.

Compiled RED2 failed at explicit stub. Initial/golden2 passed. Expanded test build caught attempted
writes to immutable UnitKey properties; constructors fixed test data. Expanded8 passed in52.216s.
Tests compare every frozen base/state/event length/hash and terminal prefix/receipt, all cuts,
original-byte retries, independent expected full World and typed resource retention. Deep mutations
cover full base and both state cuts plus event fields; re-signed event, proof-digest/profile forgery,
wrong source/actor/scope/stale commands, occupied/nonadjacent destinations, second move, malformed
bounds and captured source ownership reject. Final gates and direct oracles logged separately.

## Limits and review challenge points

Generic overflow cases cannot be reached from exact authority27/CP0 admission; altered capacity
claims reject as forged cache/base. The one-event cap intentionally excludes broader movement
routes, released-II, settled histories and positive relationships. Full predecessor replay favors
correctness over caching. Internal projections are trusted in-process carriers; readback always
requires the independent source. Check atomic member/World spending, serialized World fidelity,
source ownership, exact pins after replay, predecessor identity and retry ordering especially.

Next3m owes actual Movement completion and exception expiry. Parent017–019, settled source/progress,
later-II/consumed lineage, Snapshot/public/runtime activation remain open. No remote CI claim.
