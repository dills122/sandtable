# Author explanation

Review instance: 1 of 3. Author testimony, not a readiness verdict.

## Intent and scope

Task019B authenticates actual inherited released-I source/progress and proves a supported prospective
Combat candidate. Following control can consume the proof; this slice emits no event and does not
repeat or advance state. Settled source/progress integration remains open. Five primary files were
recorded before coding; administrative navigation and evidence are separate.

## Approach

Existing017C replay authenticates creation through held-I/no-move Combat and Release using retained
inputs/events. Two frozen3j terminal Control hashes restrict compatibility after replay. Guards
require completed authority25, current cycle1/first slot, released-I/status-none, CP0, CPA/ceiling10,
pending ordinal2 exception, unused offense, one actual I-to-none progress reference, unchanged
Movement-end locations, empty attack/obligation/settlement histories and Normal Weather/ammo10/TOE10.

Regenerate already-authenticated Created11 from retained request and call CertifyInitialProfileFacts
on projected World/current cycle. Full initial-fact comparison plus native ProveSupport yields a
supported candidate. This predicts unchanged facts after prospective empty Movement/Breakdown;
it does not emit those completions, draw RNG or mutate state.

Codec writes frozen3j fields in exact order. Existing canonical Release base/state fragments supply
cycle, member and random state. Candidate identity follows the frozen inherited-selection hash.
Four static fixture hashes/counts preserve historical contract compatibility; production loads no
fixtures. Native certification supplies executable result-support checks as well. Reader first
bounds/parses input, regenerates complete proof from source and compares exact bytes. Parsed proof
fields never become authority.

## Decisions and invariants

Strict two-terminal hashes follow frozen3j scope. Other legitimate Release timings reject rather
than widen admission. Released World matches certified initial facts; candidate remains prospective
and ordinal1 stays unchanged. Historical support labels do not imply current Snapshot/public
admission. Test chain construction is local to this test class, avoiding changes to existing helper
APIs within the five-file manifest.

World, Release, CP/ammunition/TOE, RNG, history, pending exception and authority must remain unchanged.
Actual progress must bind the accepted release event; prior Movement completion must come from
replayed predecessor history. A proof digest alone never authenticates source history.

## Verification

Compiled RED2 at stub; GREEN2 matches both frozen proofs in full (3905/3945 bytes). Expanded tests
initially failed4/8 because malformed JSON throws a JsonException subtype. Assertions corrected;
missing-history probes also isolated before deliberate corruption. Expanded8 and focused46 pass;
build clean. Full suite and oracles were running when packet was prepared.

Tests cover full proof/hash parity; actual progress hash and Movement receipt; candidate and retained
World/Release/RNG/resources/authority; four source support fixtures/counts/digest; every proof leaf,
alternate bytes and rehashed support; foreign/incomplete/fallback/changed valid timing/re-signed
history; missing/corrupt predecessors and owned source bytes.

## Limits and review challenges

Only two frozen owner-selected released-I terminals are admitted. Arbitrary timing, released-II and
fallback positive proofs remain unsupported. Internal proof construction/serialization is not a
persisted admission API; Read always requires authenticated source. No dispatcher/public action,
repeat or cycle opening added. Guarded3k follows; settled adapter, actual018 integration and remaining
017 lineage stay open.

Review source pins versus replay, native certification at a prospective boundary, retained resources,
canonical historical metadata, readback bounds, test independence and honest plan scope.
