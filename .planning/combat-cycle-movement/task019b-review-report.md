# Independent review

Review instance: 1 of 3. Fresh reviewer: armed_continuation_review. Read-only review.

## Findings

No actionable findings. Four source, test and project hashes match bootstrap. Branch and HEAD match
codex/combat-cycle-movement-plan and 5bdecbf. Unrelated untracked paths excluded.

## Plan review

Task019B matches bounded scope: actual inherited Release replay, released-I rights checks,
supported prospective candidate, canonical proof and source-bound readback. No repeat, spending,
event, public admission or Snapshot activation introduced. Settled source integration and
parent017–019 remain open.

Initial-profile certification reuse is justified: replay authenticates current World, guards require
CP0/ammo10/TOE10, and certification compares full initial facts and executes native result-support
checks. Current ordinal1 remains unchanged; ordinal2 is prospective.

## Author-claim reconciliation

All material claims confirmed against implementation and predecessor context:

- Derive invokes inherited Release replay before terminal hash and semantic guards.
- Literal pins supplement independent reconstruction of predecessor and Release events. Tests reject
  forged histories and valid alternate timing.
- Both-owner tests compare full bytes, size and hash. Python oracle independently derives assessment,
  establishing exact frozen3j parity.
- Native CertifyInitialProfileFacts invokes ProveSupport, including result enumeration and settlement
  bounds. Static metadata is not the sole support proof.
- Derivation is read-only. Tests compare World, Release, RNG and authority retention.
- Codec comments, design docs and plan distinguish historical support from runtime activation.

Preliminary concerns were recorded before reading author packet and resolved through implementation
and predecessor inspection.

## Verification

Reviewer launched no builds/tests. Executed Git diff check and four SHA256 checks: all passed.
Inspected logs: focused46 passed with0 skipped; build0 warnings/errors; Boundary81 passed; all six
frozen oracles passed. Format exit0 reported by author; empty log inspected. Full suite was incomplete
at review close.

## Residual risks

Full suite pending. Internal Proof record can be constructed by trusted assembly callers. Persisted
Read independently derives and compares complete bytes; future consumers must preserve this
source-bound admission boundary.

## Verdict

Ready with non-blocking follow-ups for bounded Task019B. Required full gate must finish before
delivery. Record result and refresh evidence. Passing checks with unchanged source need no further
review instance.

## Author response

Accept. Preserve source-bound readback in following control implementation. No source change required.
Full-suite result recorded separately before acceptance; no runtime/public activation claimed.

Closeout: full solution passed 2,437 tests with zero failures/skips. Review condition fulfilled; four reviewed source/project hashes unchanged.
