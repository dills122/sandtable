# Task009B implementation dispatch

Status: authorized after009A accepted at39dccfe; all gates passed. Internal worker, same branch/PR136.
Read task009b-dispatch-proposal.md as frozen design; canonical009 refinement remains authority.
Own only its five primary paths plus task009b-worker-evidence.md. Not alone: preserve root docs,
review packets and other edits. No commits/push, agents, fullsuite or predecessor/schema/fixture edits.
Root owns dev review, integration and three sequential fresh-context review rounds.

Implement dormant exhaustive positive trusted-facts certification, exact immutable Candidate codec,
and pure current opportunity-v2 digest. Use typed signature and complete finite enumeration in
proposal. Reuse existing Resolve/Content7/Created11 and spending helpers. All-result geometry and
resource support must execute before a candidate can return. No invented wire certificate, no
seed-selected support, no positive history admission, no costs/events/RNG mutation.
Candidate reader shape/bounds/canonical validation must precede access to trustedExpected.
Retain prior009A exact bytes/admission and tests. Capacity claims limited to supplied facts; actual
cursor/version and whole successor-root size remain later integration checks.

TDD required: focused RED for new behavior, minimal implementation, GREEN; preserve exact commands,
logs and unique binlogs. Use nativeMTP --project and --filter-class '*CombatIdentityTests', login:false,
approved localIPC. Fullsource formatting scoped to changed paths. Finish all child processes before
return. Report exact results/hashes, limitations, no parent completion claim. If research uncovers
actual contradiction or substantial architecture pivot, return evidence before implementing pivot.
