Task008 now retains selected inherited Reaction paths and a literal Snapshot12 contract with368
reference cuts. Accepted H1/H2 route one bounded, owned creation-rooted event stream through pre-cycle
opening, ordinary Movement, mandatory stop/resolution/completion and Breakdown completion.

H3 extends that same router through Reaction trigger, participant lifecycle, direct closure, active
fallback, second move and final completion. Actual strict readers validate every candidate fork and
entire remaining tail. Typed projections preserve active position, suspended Movement, window, route,
progress and complete receipts. Family tags and window hints cannot grant authority.

H3 focused129 tests and final inventory check pass; build/full format pass. All50 Reaction vectors /
34histories are covered, including32new histories; cumulative H1–H3 covers all286selected histories.
Tests include exact predecessor goldens, H0 state fields, canonical re-signed effects/actors, malformed
routing hints, conflicting forks and byte ownership. Full runtime root bytes and disabled-admission
restore remain H4. Dev review and two independent rounds passed. Cumulative gate, final review and exact-head CI
remain pending at this draft checkpoint; no H3 acceptance claim yet.

Earlier F2–F6/H0/H1/H2 acceptance evidence is retained. H0 counter-log CodeQL alert cleared through
one bounded experiment and final review4; exact-head CI passed. One main-based draft PR. H4 full
restore, later009–025/public activation, all28runtime traces and HOST-PUB-001 publication remain open.
