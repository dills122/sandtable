# Task010B preliminary lead review notes — not a final verdict

Read models, shared syntax bridge and initial codec while implementation active. Bridge preserves array order only for named C3a external syntax; default inherited behavior unchanged. Typed readers validate raw closed syntax and canonical bytes before trusted reconstruction. No finding established in these partial paths; engine/tests still incomplete and scope unfrozen.

Read-only descriptor audit compared all16 combat-selection-steps-v1.schema.json objects against codec strings:16 match. First audit command had unmatched Python parenthesis (SyntaxError), corrected command succeeded. Extra Timing/Idle shapes and transitive primitive semantics still need full review. Do not count this as executed runtime evidence or independent review.

Initial engine pass inspected replay capture, fixed creation binding,009B certification, route derivation, all eight command branches, Fold, field authorization and timing helpers against frozen oracle transition. No mismatch established. Created copied before caller Count/index; accepted event grammar checked before trustedBoundary validation; transitions pair separate supplied inputs and exact events. Duplicate Command hash precedes lifecycle/clock, actor match required; stale callbacks no-op; positiveFA rejects. This is preliminary and must be repeated against frozen source/test evidence.

Initial literal test run /tmp/task010b-literals.log:2passed/0failed/0skipped2.702s. Inspected loops comparing41 emitted events literal bytes/hash/count, five final Controls, replay/readback at all41postevent cuts, and every earlier input retry with changed valid clock metadata. Scope still incomplete pending mirrors and negatives. Minor retry comment corrected via worker: hash is Command only, actor checked separately.

Read-only transitive descriptor audit imported frozen selection oracle without running its suite and traversed World/Authority/Position/Random/UnitKey/Timing. All32 reachable object descriptors match combined existing/new codec; zero missing/different. This verifies descriptor text, not runtime primitive/order semantics or oracle execution.

Dev finding accepted for remediation: C3a env.typed rejects Route unconditionally, but shared inherited grammar accepts Route arrays. Reachable through syntactically complete World.settlements disposition/retreat/custody. Eventual trusted profile rejects noninitial World, but promised raw-grammar-before-context ordering requires early JsonException, not null-context failure. Worker asked for narrow C3a preserve-order-mode rejection, inherited behavior unchanged, and RED/GREEN raw Boundary sentry. Also requested failure-sensitive semantic-array-order tests and rehashed effect/from-position/postclose coverage from existing dispatch.

Route regression RED /tmp/task010b-route-red.log: expected JsonException, actual ArgumentNullException,1failure1.192s exit2. Narrow preserveArrayOrder-only Route/LegacyBrokenVehicleLot rejection inspected; inherited mode unchanged. Expanded GREEN /tmp/task010b-route-green.log12/0/0 3.972s. Read all added array-order sentries, seven rehashed effect mutations, wrong structural input/postclose callbacks, unavailable-before-opening complete no-attack paths. No remaining dev finding. Requested initial Control readback at original/mirrored start cuts before final freeze to make46/92 restore counts exact.
