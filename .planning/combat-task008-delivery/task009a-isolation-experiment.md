# One conditional experiment: reviewer retrieval isolation

After ordinary3, code verdict Ready but prior-report snippets leaked through CCE despite exclusions.
Root treats required independent-review process as blocked. User authorized one experiment/final4.
Read only named mcp config section headers, produced per-process enabled=false overrides for all11CCE
servers, then ran codex mcp list --json and asserted all11 disabled. PASS, exit0. Printed only names
and enabled flags; no credentials or transport settings. Global config unchanged. Temporary argv
/tmp/task009a-review4-cce-config.json applied to final isolated ephemeral CLI invocation.
Final reviewer must use exact source fallback, persist blind ledger before author/checks, avoid all
prior reports/history and perform focused checks. No production edits. If final4 leaves blocker,
stop for intervention; no fifth review or reset. Candidate39dccfe/hashmanifest unchanged.
