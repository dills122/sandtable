# Author Explanation — F1 actual Reaction trigger

## Intent and plan
Task008 F1 extends genuine first ordinary E1 move with the return Move4 opening one opposing
Reaction opportunity. Full creation/opening/Weather/stage/emptyReserve history remains mandatory.
Five primary files: new immutable models, projector, codec, focused tests and fixture link.
F2–F6 graph pins actual selected participant/exit histories; initial H covers all implemented cuts,
with later009–019 handlers extending same restore contract. No all28 runtime/publication claim.

## Flow and invariants
Replay accepts exactly one E1 move, verifies actual CP2/version12/rear/route boundary, then at most
one trigger event. New input is unchanged Movement2. Authorize derives exact owner/identity/version/
unit/origin/destination command before retry lookup. Replay re-emits and compares whole event bytes.
ReadState requires retained trigger and complete canonical equality; pretrigger serialization delegates
actual E1 state, matching oracle distinction. No caller state or cached window can admit authority.

Emit validates selected ordinary nonmotorized independent infantry/Clear2 profile, discovers opposing
adjacency before eligibility, then freezes one eligible opposing representation. Pure persisted
CampaignReactionIdentity functions derive Move4/committed13 window and opportunity IDs. New typed
position/trigger/window avoid legacy materialized Movement and Move2/3 constructor restrictions.
Existing route AtLocation preserves route ID/start12/origin; Reacting carries ResumeRoute and null
reactor route. Spending kernel updates CP2→4 and current element/representation; typed World,
member CP, track, receipt, prefix and actual progress append atomically. RNG/Cohesion/resources/
other unit/Reserve history and symbolic suspended position remain unchanged.

Codec retains exact Move4 field order and appends authority/receipt under irt domain. State writer
reconstructs expected postWorld from trusted E1 history and full equality-checks typed World before
bounded absent-field serialization. Existing E1 writer correctly rejects this enemy-adjacent return;
it is not relaxed or fed a fabricated second E1 event. No old model/contract edits.

## Evidence and review
Frozen two-owner traces, two events, four replay cuts and eight byte/hash artifacts. Tests assert
independent persisted ID preimages, exact route start/origin/ID, CP4/version13/opportunity/flow,
authorized retry, wrong actor/envelope/occurrence/unit, raw leaf/canonical mutations, re-signed
window/adjacency/route/CP forgeries, wrong caches, missing/foreign/unsupported predecessor and buffers.
Dev test read corrected independent opportunity preimage key to windowId; contract unchanged.
Dev source review added direct typed opponent-CP World guard probe and terminal Command rejection;
saved accepted input remains exact retry path. CA1822 constant getter findings corrected to immutable
auto-properties. Final executed checks in f1-evidence.md.
TDD red missing types recorded /tmp/f1-red.log; final focused15/full2,051/build/format and review evidence now complete in f1-evidence.md.

## Costs, limits and challenge points
Bounded duplicated state/World layout trades local maintenance for strict inherited compatibility.
Every replay rebuilds short actual history; no arbitrary history/performance claim. F1 stops before
participant movement, closure, multiple opportunities, vehicle/Reserve profiles, public handles,
activation, Snapshot12 composition or durable publication. Future F2 may extend internal window
shape for active/resolved opportunity while preserving F1 bytes. Review trust boundary, discovery
ordering, persisted IDs vs public capabilities, World guard and progressive H dependency claims.
