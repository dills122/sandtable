Task008 now replays selected inherited Reaction paths from trusted creation-rooted history, including
participant movement/completion, direct closure, active fallback and second-move completion. The H0
contract defines 368 literal Snapshot12 cuts. Accepted H1 derives creation, preamble, Weather, stage
and Reserve partitions from one bounded, owned retained stream.

H2 extends that stream through ordinary Movement, mandatory stop/resolution/completion and Breakdown
completion to Combat entry. Typed Reserve completion establishes the boundary; existing strict family
readers validate exact event bytes and effects. Same-tag unsupported Reaction events and extra tails
reject. Full literal root serialization and disabled-admission restore remain H4.

H2 validation: 70 focused tests; all64 assigned fixture cuts/48distinct histories, exact predecessor
state/event goldens, full state/ledger comparisons, ownership and genuinely re-signed causal forgeries.
Build/format, cumulative2,174 and boundary81 pass. Dev review, all three independent rounds and exact-head CI passed at4a43ff4. H2 is accepted. Earlier F2–F6/H0/H1 review evidence remains retained; H0's remote CodeQL
counter-log alert was cleared by one bounded experiment and final review4, with exact-head CI passing.

One main-based draft PR. H3 Reaction routing, H4 full restore, later009–025/public activation,
all28runtime traces and HOST-PUB-001 durable publication remain open.
