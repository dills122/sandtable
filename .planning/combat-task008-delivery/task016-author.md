# Author Explanation — Task016 closure

## Intent And Success Criteria

Extend accepted dormant Task015 Result2 frontier through original-participant relationships, round closure and one Close Assault completion into Reserve Release. All32 retained synthetic contexts must match272 exact event literals,304 causal state hashes and32 complete final-state JSON literals. This closes bounded Result2 settlement, not public Combat or actual creation-rooted positive campaign history.

## Plan-To-Implementation Traceability

Canonical Task016/checkpointG owns raw-Retreat suppression of Engaged, original-only participants, adjacency, complete immediate settlement before CA and retained future duties. Explicit execution refinement permits five material paths plus three dependent regression-maintenance paths. Task017 Release behavior and Task018/019 movement/repeat remain excluded. Source base d600966; frozen candidate/pins and checks recorded separately. Implementation acceptance awaits root gates and fresh reviews.

## Technical Approach And Flow

Unchanged Apply/ReplayTrustedBoundary APIs authenticate separate Created/C3a predecessor/Round2/Result2 histories. Replay reaches paid immutable Context, rederives original result and loss/retreat/custody consequences, then advances three structural events with exact version, null time and available clock. Relationships use current original element locations and authenticated Content edges. Raw Engaged applies only without required retreat; refused required retreat still yields Contact if adjacent. Nonadjacency still publishes a null relationship receipt.

Round closure collects ordered disposition/losses/retreat/optional-custody/relationships receipt IDs. CA references that event's actual receipt plus original fifth committed step and official authenticated Combat-to-Release topology. Terminal metadata is assigned after canonical event digest, before state serialization. Retry and stale/no-window callbacks precede Closed guard, preserving all accepted retries after closure without permitting new actions.

## Changed-Component Walkthrough

- Resolution: immutable closure metadata/origins, owned proof effect, three structural transitions and terminal guard.
- ResolutionCodec: exact existing grammar writers; status/window/receipt agreement; full typed World comparison; recomputed structural terminal-event identity and prefix chain.
- Obligations: named immutable relationship append through existing unconditional hashed settlement/causal chain validation.
- LossRetreat: converge independently reconstructed noncapture/custody frontiers, derive original-only relation, compare complete retained settlement and preserve all assets/resources.
- New Closure tests: complete literals/hashes/final JSON, all-cut replay/retry/discard, negative mutations, typed World and terminal-metadata forgeries, raw-before-context and ownership.
- Prior LossRetreat/Custody tests: allow exact next relationship only at eligible frontier; preserve144/176 and176/208 golden boundaries and later skipped-history rejects. Resolution tests retain assertions; only obsolete comment changes.

## Decisions And Rejected Alternatives

Reuse existing projection rather than adding another authority module. Do not relax World7: it intentionally allows later element movement after relationship publication, so Result2 must continue comparing the complete reconstructed World. Use actual original locations, not guard membership or pre-loss location shortcuts. Reuse official topology and frozen contract bytes rather than adding new protocol versions.

Development review reproduced a typed serializer defect: positional last-receipt checks alone accepted a relationships state relabelled round-closed. Minimal immutable internal prior-prefix/version origins now let codec recompute actual expected terminal events and validate receipt/event/command hashes, actor/version and prefix linkage. Raw event-prefix framing includes event bytes, so hashes alone could not reconstruct predecessor prefix. These origins are replay-derived implementation facts, not serialized authority or a raw JSON cache. Full separate-history replay remains authoritative.

## Invariants And Boundary Conditions

Original paid Context, result/RNG, attack/target history, current resources, custody lots/guards/entitlements and future obligations survive closure unchanged. Relationship endpoint UnitKeys remain original participants. Captured allocation requires custody first; all immediate receipts appear once in order; future duties do not block CA and are not executed. Exact retries recover original event with current state after closure. Fresh commands reject; stale callbacks remain NoOp. Proof collections and returned bytes are copied. No caller prior-state Apply path or remote/model I/O introduced.

## Verification Performed And Results

Worker evidence and root checks provide exact commands and frozen results. Meaningful first RED: accepted015 frontier rejected literal relationship advance. First GREEN passes all32 contexts. Separate serializer regression RED: borrowed relationship receipt could invent round closure; fixed with typed origins and expected-event identity checks. Final worker102/102 focused/shared plus73/73 rules passed, zero skipped; scoped format and diff checks passed, eight source pins frozen. Full272/304/32 evidence and both REDs retained in worker report. Root integration checks are running; independent reviews have not started.

## Risks, Tradeoffs, And Maintenance Costs

Internal terminal origins add two nullable implementation fields to state and bounded canonical event recomputation during serialization. They restore through replay and leave persisted bytes unchanged. Topology/proof derivation appears in transition and validation; validation deliberately independently recomputes expected semantics. Existing fixed initial turn/selected profile remains limit. Tests retain expensive cumulative replay to make recovery/identity failures visible.

## Deviations, Deferrals, And Known Gaps

No schema, fixture, World7, receipt model, topology, public registration or project-file change. Eight source/test paths remain explicit ceiling; administrative docs/evidence counted separately. Dormant synthetic trusted Boundary proof does not establish all28 actual creation-rooted runtime histories, Snapshot successor, durable HOST-PUB-001 publication, public activation or future feeding/training. Task017 remains separate and needs positive predecessor work beyond any isolated native Release kernel.

## Challenge Points For The Reviewer

Review full source/plan independently before this testimony. Verify full World equality after relationships, original-only adjacency/raw-Retreat semantics, required custody/proof ordering, actual terminal event receipt binding (including borrowed IDs), closure retry/callback ordering, raw syntax before trusted context and preservation of earlier frontiers. Distinguish runtime golden assertions from contract-only evidence and actual campaign/durable integration claims.
