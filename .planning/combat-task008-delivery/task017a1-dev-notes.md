# Task017A1 development review — in progress

Base afdcd9e. Root inspected compiling-skeleton intent and independent44-row base builder before implementation: fixture hash pinned; exact11 isolated families and4 historical rows separated; expected hashes do not construct production values. Probe creation uses retained Created11 setup/config, Content7 own IDs and Result2 cycle template with explicit synthetic overrides. Collections copied into read-only wrappers; nested values are immutable records. No lifecycle introduced.

No verdict on pending codec implementation or negatives. Root will inspect complete final four-path diff, strict raw-before-context behavior, expected-base trust boundary, cycle/topology binding, complete history semantics, primitive grammar and ownership before freezing candidate.

Optional CCE recall/context query rejected by automatic approval review; root uses local source inspection and local decision records. No external retry.

19:12 UTC meaningful behavioral RED verified directly in /tmp/task017a1-red-behavior.log: compiling skeleton serializes empty object, first frozen first-release/Axis/first-slot base hash differs (expected553d6c…, actual44136f…); native MTP1failed/0passed exit2. This is canonical behavior mismatch, not missing-symbol compile failure. Worker separately records preceding analyzer corrections. Codec implementation and expanded negatives pending.

First complete codec draft reviewed19:13UTC. Two concrete development findings sent to worker before any independent review: raw shape check still admitted whitespace/escaped-equivalent spellings/negative-zero until after trusted expected/context access; inherited AttackHistory.cycleId was checked as ID rather than hash. Requested focused regressions before fixes, including malformed raw with null trusted inputs and unreferenced malformed attack record. No extra source paths needed. Duplicate null-array validation noted as routine cleanup. Random/attack values otherwise follow frozen primitive grammar; exact expected bytes provide contextual binding, not lexical hashes alone.

Final source/test development review19:18UTC: models, complete codec and test builder/negatives read, fixture-link-only project diff checked. Raw canonical spelling and inherited attack cycle hash issues corrected. Primitive-aware field mutations now reach expected-byte mismatch rather than failing malformed hash/side checks. Raw parser-derived JsonReaderException is legitimate JsonException subtype; tests use ThrowsAny rather than production normalization. Release vocabulary explicitly none/I/II; frozen golden parity protects formatter reuse mistakes. Legal II history/overspend, expired movement, offensive links,32members/512attacks and owned collections covered without implying campaign provenance. No remaining correctness, architecture, ownership, input-boundary or bounded-cost finding. Await worker final format/pins, root full gates and fresh independent reviews; not accepted yet.
