# Task011 scope research — not dispatched

Research while010B implementation is active.010A accepted2eda2ed/6e6dd6f;010B/C remain prerequisites. No011 implementation authorized by this note; root must freeze concrete file scope after parent010 acceptance.

Canonical plan Task011 owns two private slots, duplicate readback, fixed deadlines and Prepared continuation; Task012 separately owns debit/commit. Active contract is combat-sealed-round-v2, not historical v1. Read v2 prose/schema/oracle and canonical Task011/012 rows. Existing010B trusted Boundary/input history must be replayed to accepted decline at Force Assignment; a caller Base/Control cannot replace it. Both sources remain explicitly synthetic/trusted mechanism evidence until an actual positive-history adapter exists.

## Contract boundaries and reuse

Base2 carries Boundary, replayed C3a Control and explicit ClockConfiguration2. Validate supplement against original Config1 force-assignment budget and fixed public-opening-clock.v2 policy; preserve original Config1/Rules10 and both hash bindings. Reuse009B opportunity-v2 ordered preimage after authenticated Base2. Never convert active v2 state/events to v1 or call v1 transition with reshaped state. No historical v1 production reader is implied by this slice.

Replay must pair independently trusted accepted RoundInputs with untrusted event bytes and rebuild predecessor before local transitions. Like010B, never recover trusted authority by parsing event.input. Own Created before list access; bounded16 local events,1MiB/depth32/512. Raw parse/closed grammar/canonical first, trusted replay then whole bytes. Use private transition and immutable models; public Apply cannot take arbitrary prior Control as authority.

Task011 can implement exact precommit subset: open/seal/expire/unavailable/complete-step; retain RoundState2 fields for costs/history with empty values. Prepared completes FA and certified empty AA to step5; positive CA completion and commit-attack reject pending012. Cancellation completes remaining three steps and closes without costs/history/RNG. Explicit commit-family rejection must not look like full Round2 implementation. Four committed fixture traces yield accepted prefixes ending before debit; six cancellation traces are complete. Later012 can extend same closed module without changing prior bytes. This is proposed task-boundary mapping, not an accepted new architecture or dispatch.

## Critical ordering

- Primitive/context/command fields/actor category precede command hash retry. Exact command+owner retry returns original event before status/time gates, including unavailable time and later terminal recovery. Changed consumed choice rejects.
- Stale System expiry/unavailable or non-Collecting status: no-op. Prepared wins race; structural continuation needs null time/available=true.
- Seal must validate live own slot and exact allocation before clock handling. Valid malformed/foreign/stale inputs cannot trigger system cancellation.
- Fresh valid seal unavailable/null/below public opening floor -> system-authored cancellation, while receipt actor retains authenticated input actor. Equality/after deadline rejects player seal; System expiry may cancel. Available opening<=time<deadline accepts.
- Floor/open/deadline immutable. Opposite seal4000 then own3500 after opening3000 both legal. No shared private high-water/per-side invented floor. Trusted round opening1999 after predecessor RBA2001 is legal; independent confidence, not private predecessor timestamp, controls opening.
- admissionEnabled=false rejects fresh open only; replay of accepted history remains possible. Exact retry, Prepared/cancelled structural recovery remain available. No host clock monitor/public revision claim here.

## Literal inventory (read-only audit)

Frozen v2 fixture has10cases,58events,68stored states including starts. Four committed cases each6events/7states; six cancellation cases contain34events/40states. Task011 prefixes exclude exactly four final attack-committed events:54 literal events and64 literal state cuts. Unlike010B, fixture stores every RoundState cut. All ten Base2 literals and independently supplied C3a predecessors must validate. Include both actors/seal orders,24clockOutcomes, own-visible equal-outcome probes and all retry/restart cuts. No fixture regeneration.

Audit command initially assumed final state was a single string and failed TypeError; corrected audit inspected list and counted each retained state. No source/fixture modifications or production checks run.

## Open research before dispatch

Pin five primary files after010B API freeze (likely models/engine/codec, narrow010B codec bridge, CombatSealsTests; csproj already links Round2 fixture from009B). Verify no duplicate World/Boundary grammar or mutable World authority. Define exact allowed predecessor closure and first-side/role handling. Read legacy.read_base semantic conditions rather than import its implementation. Determine local schema inventory and shared codec bridge bounds. Build evidence matrix for all malformed/rehash/privacy/capacity/retry cases and explicit012 rejection. No architectural blocker established, but this note is not implementation-ready dispatch.

Follow-up source check: legacy read_base requires selected outcome, non-null decline, exactly stepIndex3 and three step receipts, not closed, and Candidate exactly derived from independently replayed Boundary. V2 adds exact ClockConfiguration supplement. Runtime011 should express these conditions directly on010B replay result, without introducing a legacy v1 transition engine. Explicit opening1999 after RBA2001 is contract-required v2 behavior.
