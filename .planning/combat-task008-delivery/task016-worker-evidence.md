# Task016 bounded worker evidence

2026-09-20. Base `d600966`, branch `codex/combat-custody-closure-session`. Implementation scope: five material files (Resolution, ResolutionCodec, Obligations, LossRetreat projection, new CombatClosureTests), three narrow dependent test maintenance files. Root owns docs, commits, full gate and reviews. Eight source/test pins in `task016-source.sha256`; this evidence and pin file are separate administrative artifacts. Concurrent root session-index edit preserved.

Applied skills: `/Users/dsteele/repos/ai-central/templates/skills/imported/agent-skills/test-driven-development/SKILL.md` and `code-review-and-quality/SKILL.md`. CCE session recall/context search used before named source reads. Local decision record retained here; no external memory write attempted after prior recorded auto-review rejection.

## Implementation

Original participants derive relationship from actual postsettlement locations and authenticated Content edges. Raw retreat suppresses Engaged including refusal; nonadjacency emits mandatory null relationship receipt. Named immutable hashed append requires disposition/losses/retreat and custody iff positive capture. Existing projection finalization reconstructs all resources through custody before relationships; full typed World equality remains required through final closure.

Round proof owns copied ordered disposition/losses/retreat/[custody]/relationships IDs. Official authenticated boundary topology supplies Combat index5→Release index6; CA references committed fifth step and actual round receipt. Receipt digest assigned before serialization; CA atomically sets closed and own receipt. No RNG/payment/resource/future-duty execution. Retry lookup and stale/no-window callback NoOp precede closed rejection.

Root dev review identified forged serializer promotion using real relationship receipt as invented round receipt. Meaningful RED confirmed; fixed using immutable typed terminal origins (prior version/prefix) retained by engine. Codec recomputes expected canonical terminal effect/event, receipt/command/event hashes, actor/version, terminal position and linked round→CA prefixes. No retained raw event cache or canonical schema change. Serializer consistency supplements full independently trusted replay; it is not arbitrary-state admission or a supplied-state Apply API.

## Tests and commands

All .NET commands used `login:false`, approved local IPC, native MTP `--project`, no restore (root restore already passed), unique binlogs under `/private/tmp`. No full suite run by worker.

1. RED: `dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --no-restore --filter-class '*CombatClosureTests' -bl:/private/tmp/task016-red.binlog` → exit2, 0/1. Actual accepted015 terminal frontier's next literal relationship advance rejected with `Relationships and closure require later tasks.` at old Resolution.cs281. Production unchanged at RED.
2. GREEN first draft: same command with `task016-green1.binlog` → exit0, 1/1; test iterates all32 literal relationship successors.
3. Expanded closure: same command with `task016-focused2.binlog` → exit0, 6/6, 47.554s. Exact272 event literals,304 hash/readback cuts,32 whole final JSON literals;16contact/4engaged/12null,16proof4/16proof5.
4. Root review RED: `dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --no-restore --filter-method '*BorrowedReceiptCannotInventTerminalStage' -bl:/private/tmp/task016-stage-red.binlog` → exit2, 0/1; `Assert.ThrowsAny() Failure: No exception was thrown`. Fabricated round stage borrowed actual relationship event receipt.
5. Invalid filter attempt: `dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --no-restore --filter-class '*CombatClosureTests|*CombatCustodyTests|*CombatLossRetreatTests|*CombatResolutionTests' -bl:/private/tmp/task016-focused3.binlog` → exit5, zero tests. Native xUnit filter uses repeated options, not pipe alternation; this is not test evidence.
6. CLI help attempt: `dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --no-build -- --help` → exit134, CLI failfast `An unexpected help-related message was received when --help wasn't used.` No test result claimed; subsequent explicit process inventory found no orphan from this attempt.
7. Revised focused: `dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --no-build --filter-class '*CombatClosureTests' --filter-class '*CombatCustodyTests' --filter-class '*CombatLossRetreatTests' --filter-class '*CombatResolutionTests' -bl:/private/tmp/task016-focused4.binlog` → exit0,30/30,71.335s. Includes terminal-evidence fix and both fabricated promotions; final added origin/hash/actor/version negatives run in final shared command below.
8. Final focused/shared: `dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --no-restore --filter-class '*CombatClosureTests' --filter-class '*CombatCustodyTests' --filter-class '*CombatLossRetreatTests' --filter-class '*CombatResolutionTests' --filter-class '*CombatWorldTests' --filter-class '*CombatIdentityTests' --filter-class '*CombatStepsTests' --filter-class '*CombatSealsTests' --filter-class '*CombatCommitTests' --filter-class '*CombatAdjudicationTests' -bl:/private/tmp/task016-focused-shared-final.binlog` → exit0,102/102,87.237s, zero skipped. Includes final7 Closure tests with added origin/hash/actor/version negatives. Adjudication class pattern matched no class; actual frozen rules covered separately.
9. Direct rules: `dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --no-build --filter-class '*CombatSelectedRulesTests' --filter-class '*CombatRulesInputArtifactTests' --filter-class '*CombatRulesetManifestTests' -bl:/private/tmp/task016-rules-final.binlog` → exit0,73/73,977ms.
10. Scoped `dotnet format Sandtable.slnx --no-restore --include` eight pinned paths → exit0; repeated with `--verify-no-changes` → exit0. `git diff --check` → exit0. Sandbox process inventory initially denied `ps`; approved read-only retry succeeded.

Coverage includes all-cut accepted retries and discarded candidates including final closed; original event recovery/current-state identity; fresh advance/resolve/choose rejection and stale callbacks; duplicate suffix rejection; structural exact version/null time/available clock; rehashed receipt, relationship, proof missing/reordered/duplicate/foreign, CA topology/step/round, World and future-duty forgeries; typed postrelationship CP99 World construction succeeds but serialization rejects; borrowed terminal IDs and missing/tampered terminal evidence; raw-before-trusted-context payload/bounds/canonical rejection; owned proof and event byte buffers. Existing32/64,144/176,176/208 goldens preserved and executed in dependent suites. Resolution skipped-prefix assertions unchanged except stale comment; Custody End remains first relationship event.

## Gates and limitations

Worker result remains candidate, not acceptance. Root full build/test/Boundary/format, development review and three fresh sequential independent reviews remain required. No Task017, actual positive creation-rooted history, Snapshot successor, HOST-PUB-001/public activation or future-duty execution claim. No contract/schema/fixture/World7/receipt-model changes. No worker commits, subagents or full suite.

Final worker closure: eight SHA256 checks all OK; final diff check exit0. Approved `ps` inventory found no Task016 `.NET test`, testhost or format process (only inventory shell/rg itself). All worker sessions completed; root may start full gate without concurrent worker writers. Final scoped verification175/175 across102 focused/shared plus73 rules, zero skipped. No outstanding worker blocker.
