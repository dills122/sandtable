# Author Explanation — F2 participant lifecycle

## Intent and plan
Task008 F2 continues actual F1 trigger through reacting move3, participant completion3, mandatory
System empty stop resolution2 and no-eligible close3. First participant cut remains available for
later F4/F5 branches; F3 direct closure branches from F1. Five primary files add new models,
projector, codec, tests and fixture link. Canonical artifacts and F1 source remain unchanged.

## Flow and choices
Full retained creation/E1/F1 history reconstructs initial state13. Replay permits at most four suffix
events, derives each current Command and canonical transition and compares full bytes. Apply checks
command shape, reacting-owner or System actor, creation/cycle before consumed-version lookup;
exact accepted input returns original event/current terminalstate, changed reuse rejects.
ReadState compares complete replayed canonical bytes. Commands stop at terminal17.

Window overlay retains immutable F1 trigger and separately tracks active/resolved IDs. Reactor move
selects sole opportunity, creates actual route at14 and updates own CP0→2/World/track/receipt/progress.
Completion at15 resolves participant and creates actual ReactionCompleted stop. Empty resolution16
consumes reactorroute but leaves inactive window. Close17 requires exhausted opportunities and
restores exact suspended phasing route/position. Phasing CP4 and all nonreactor resources unchanged;
only move adds materialprogress, no RNGdraw or implicit segment completion.

Public handles differ from persisted IDs. Window binds trigger13/reactingside; opportunity binds
currentversion and full sorted legal moveoptions. Frozen ordinary Content7 geometry yields one
assault→rear option then still-legal rear→supply option; completion must include latter in capability.
Reuse pure capability/opportunity identities, action types, route/stop/flow primitives and spending.
Public-window helper tied to historical rules hash is not weakened; local exact ordered hash used.
Bounded World writer rederives full expected lifecycle World before hardcoded absent fields.

## Verification and dev review
Canonical two traces/eight events/ten cuts/28artifacts. Root oracle baseline passed; worker TDD red
missing types /tmp/f2-red.log. Root preliminary models/projector/test review found no blocker;
requested explicit nonempty completion option inventory and correct event-specific receipt hashes.
Expanded checks exposed test harness assumptions: track collection reference equality across fresh
replay, and shape-only codec expected to reject changed yet valid source atoms. Worker confirmed
and corrected to structural track equality and actual Apply authority validation; production logic
unchanged by those failures. Final all-actor/cross-kind negatives included before freeze.
Root full format caught test import order; cosmetic swap applied and hash refreshed during round1,
with reviewer notified. No semantic source change or altered test coverage.
Final focused/integration results and independent reviews recorded in f2-evidence.md when complete.

## Costs and limits
Internal immutable wrappers and duplicated bounded codec layout preserve prior family guards;
future family expansions must keep exact canonical parity. Short retained history is replayed for
admission and World guard; no arbitrary capacity/performance guarantee. No chosen second move,
direct/active fallback, multiple opportunities, positive vehicles, public registration, general
Snapshot12 or durable publication. F/H and HOST-PUB-001 remain open. Review capability inventory,
actor-before-retry, mandatory stop, actual phasing resumption and unchanged unrelated authority.
