# Task017 Reserve Release feasibility — research only

## Decision packet

2026-09-20, worktree `/Users/dsteele/.codex/worktrees/8fcc/sandtable`, branch `codex/combat-custody-closure-session`, observed HEAD `ba54efd720b38627b473cfd5d2e2ba4ee638d6b2` (concurrent root documentation edits may exist). Decision owner: root implementation coordinator. Question: smallest dependency-valid Release implementation slices after Task016 acceptance, with first/later/empty behavior and honest cumulative replay provenance?

**Recommendation: split before edits.** One parent-complete Task017 within five primary files is not supported by current APIs. Separate (A) private Release kernel/typed history, (B) native empty Release after accepted Task016 result history, and (C) positive creation-rooted Reserve lineage/admission, with missing 3h predecessor implementation separately bounded before C. Do not close parent017 from A+B or synthetic first/later tests. Task018/019 own movement/exceptions and real repeat/finish, not017. Root must reconcile prerequisite ownership against canonical plan; research does not authorize implementation or silently add predecessor work to017.

Scope: read canonical plan, frozen contract/spec/oracle/fixture and current dormant source/tests; parse JSON only. No source/test changes, .NET/full oracle execution, fixture generation, commits, reviews, subagents, storage/content spike or external CCE memory writes. Only this artifact written. Research-to-decision skill previously read at `/Users/dsteele/repos/ai-central/templates/skills/first-party/research-to-decision/SKILL.md` applies. Success: distinguish kernel semantics from authenticated reachable admission, identify exact evidence types, propose bounded paths and next gate. Stop: retained decision within15-minute timebox.

## Sources and method

Source hierarchy: current canonical plan/roadmap → frozen Release/World/cycle contracts and oracle source → current C# APIs → retained fixtures. Sandtable `context_search` used for source discovery; exact named files read afterward. No memory write attempted. Source lines refer to inspected working tree, not future016 APIs.

- `docs/design/combat-cycle-implementation-plan.md`: five-primary-file split rule near95–99; checkpointH/Task017 near936 after concurrent016 refinement. Task017 requires release window, canonical own-unit dispositions, pinned budget, first-I conversion/later-II retention fallback, explicit completion, consumed convert/retry/stale/expiry/cumulative ceilings, no auto-release/repeat. Dependency016.
- `docs/specs/combat-reserve-release-v1.md` and matching `.schema.json`, `verify-combat-reserve-release-v1.py`: private native kernel, full typed history, actor/timing/retry/receipt/world boundaries. Main reference functions `validate_base:84`, `transition:171`, `project_world:271`, `trace:289`.
- `docs/specs/combat-inherited-reserve-cycle-v1.md` and matching oracle/fixture: actual both-owner held-I 3h ten-event route; golden inventory function near421.
- `docs/specs/combat-inherited-reserve-release-v1.md` and matching schema/oracle/fixture: exact positive3i open/release-I/complete, source-derived base near159, full-history apply near301.
- `docs/specs/combat-result-cycle-finish-v1.md` and matching schema/oracle/fixture: accepted native Result2 empty Release bridge; checked source near136, derive near175, retained vectors near684. Only first two native events belong to017.
- `docs/specs/combat-cycle-control-v1.md`: completed Release prerequisite, actual progress, pending exception ownership and019 repeat/finish boundary.
- `src/Cna.Core/Campaigns/CampaignCombatReserveModels.cs:9–12`, `CampaignCombatReserveCodec.cs:148–154`: designation-only history; future fields hardcoded null.
- `CampaignCombatReserveOpening.cs`: creation/preamble/weather/stage/designation/completion replay, actual held-I first opening exists.
- `CampaignCombatInheritedMovement.cs:64–69`, `:103`; `CampaignCombatMovementLifecycle.cs:10–17`; `CampaignCombatCertification.cs:10–49`: current route cannot consume held-I/no-move3h.
- `CampaignCombatHistoryReplay.cs:40–65`: routing after first opening enters ordinary Movement then three-event route lifecycle; no3h family branch.
- `CampaignCombatInheritedSelection.cs:42–53`, `CampaignCombatInheritedNoAttack.cs:38–64`: no-attack traversal reuses current restrictive G2 admission; not generic positive Reserve adapter.
- `tests/Cna.Core.Tests/Cna.Core.Tests.csproj`: existing Result2/designation/inherited ordinary fixture links, no Release/3h/3i/result-cycle bridge link.

## Authority facts and implementation gaps

**Documented fact:** ReleaseBase requires exact cycle/actual acting side/slot/position, terminal version/prefix/CA receipt, independently authenticated World hash/RNG/stage attack history and complete ordered own members. A digest alone authenticates none of these. Native profile `isolated-ledger` intentionally uses synthetic receipt/history/World probes, including extra members and CPA9. `settled-empty-release` means no pending work, not necessarily zero members.

**Observation:** current typed `CampaignCombatReserveHistory` has scope/designation only; current codec emits null conversion/release/ceiling/offensive/exception fields. Native Release needs full immutable history and movement-exception values. Avoid widening designation writer or forcing existing immutable designation state to carry later mutations: new Release-owned full history/member types can explicitly map trusted predecessor values without changing earlier contract bytes. Reuse existing UnitKey, CP, authority, receipt and timing value types where wire semantics match. This is recommended implementation shape, not frozen API decision.

**Observation:** held-I first opening already replays in `CampaignCombatReserveOpening`. Downstream `CampaignCombatInheritedMovement` rejects any element with Reserve status other than None. Lifecycle rejects `moves.Count==0`; certification accepts only6/7 moves and acting CP12/14. Existing no-attack replay depends on that certification. Therefore existing ordinary G2/no-attack API cannot authenticate3h by simply passing empty move arrays or supplied terminal World. Positive3i adapter has genuine missing prerequisite implementation.

**Documented fact:** frozen3h consumes real creation→preamble→Weather→stage→designation→opening, then owner direct Movement completion3, System idle Breakdown completion2, empty selection open/close2, six no-attack completions. It retains held-I, CP0, designation history, World/RNG, Movement-end proof and no material progress. Frozen3i derives version22 ReleaseBase and applies22→23→24→25, first-slot both owners, one release-I with pending ordinal2 exception; no conversion positive lineage or later-II lineage.

**Inference:** private first/later/consumed history behavior can be implemented and tested now behind explicit trusted/probe boundary; it cannot be admitted as actual campaign behavior merely because kernel validation accepts self-consistent history. Later-II release and retained consumed offensive history require independently authenticated earlier conversion/release/Movement/cycle provenance when integrated. Actual alternate conversion and later runtime admission remain separate gates. Do not synthesize a Reserve participant from Task016's attacker, which has already performed selected assault.

**Provisional after016:** derive empty ReleaseBase by replaying separate independently trusted Created/C3a predecessor/Round2/Result2 tuples through accepted016 API, requiring closed result, no window/immediate settlement work and exact CA completion receipt. Preserve synthetic source provenance. Actual acting original member has status none, CPA10, exact spent CP; retain attack history/World/RNG and all guards/entitlements/future obligations. Native Result2 bridge starts new Release clock scope at `acceptedHighWater=null`; do not copy Result2's private owner acceptance into it. Historical Result1 settled-empty probes use different source/high-water construction and are not adapters for Result2.

Result2 bridge source policy admits named32 branches with reliable owner-choice effects; authenticated source fallback effects are outside bridge even if World is identical. A future adapter using bridge vectors must preserve that restriction or explicitly cite a different already-frozen admission contract; no unsupported source-family widening. Full bridge synthetic pre-retreat Movement certificate belongs to cycle-control source, not creation-rooted evidence and not necessary authority invention for native empty release. No full bridge completion, repeat, Snapshot or host claim in017.

## Minimal017A contract/type constraints

Native target is exactly `combat-reserve-release-v1.schema.json` and `sandtable.combat.reserve-release.v1` / `sandtable.combat.reserve-release-receipt.v1`, closed contractVersion1. New dormant class is a specific Reserve Release implementation, not generic policy/state-machine framework or externally admitted arbitrary-base API. Internal kernel consumes locally authenticated/probe-certified context; production source adapters independently derive exact base and are sole integration boundaries. Test-only isolated fixture construction must not become gameplay admission. Freeze source hashes and schema inventory in verification; never select protocol by filename alone.

Existing useful types: `CampaignCombatUnitKey`, `CapabilityPointAmount`, `CampaignCombatCycleAuthority`, `CampaignOpeningPreambleReceipt`, `CombatStepsTiming`, `RandomStreamState`, `CampaignElementReserveStatus`; existing `CampaignCombatSpending` already distinguishes ReleasedReserveI/II ceilings. Reuse where exact native wire semantics match; do not reinterpret prior designation-only `CampaignCombatReserveHistory` as full release history. Nested immutable Release-owned Scope/History/MovementException/Member/Base/State/Effect values in one specifically named kernel file avoid adding separate model-file overhead or touching unrelated designation codec. Codec and focused tests remain separate. Kernel+codec+tests+csproj+plan are exact proposed five primary paths; root alone edits plan and decides dispatch. If source/type size invalidates this bounded arrangement, split before editing.

Fixture first cases: first-release, first-convert, first-timeout-midway, first-clock-regression, first-unavailable, first-opening-clock-loss. Later: later-release-retain, later-complete-intent, later-expiry. Empty/preserved: empty, prior-release-retained, settled-guard-empty, settled-escape-empty. Consumed convert is lifecycle invariant (conversion removes original from queue), not separate retained case named consumed-convert; require focused attempted revisit, exact retry and postcompletion retry checks. Historical settled guard/escape use Result1 source and cannot establish native Result2 adapter parity. Current typed Result2 context/clock provenance remains separately preserved in017B.

## Required kernel semantics

**Documented facts, implementation obligations:**

- Original own UnitKeys sorted, unique, max32; exact turn/stage/slot/actual side, reduced CP, positive CPA. Queue fixed from opening I/II members. First ordinal permits I release-I/convert-to-II, later permits II release-II/retain-II; reject II-first or I-later history. Status none preserves prior release history and has no decision.
- Converted member consumed once; cannot revisit converted II in same Release. Later owner `complete-release` retains remaining optional II in one System-authored completion; first release cannot skip I. System `complete` only after queue empty. Empty membership and singleton-none both explicitly open then complete; never auto-release on arrival.
- One Config-pinned `reserve-release` budget/deadline, one decision for nonempty queue. Empty has no timing/decision. Reliable high-water persists; equality at deadline rejects owner action. Clock loss/regression or explicit unavailability deterministically locks fallback. First fallback converts unresolved I in order, drained by `fallback-step`; later fallback retains rest in one completion. No late owner interleaving, budget renewal or remote inference.
- Expiry after final disposition but before completion emits completion only; exact retry returns old receipt, different stale timer is NoOp. Actor/shape/identity validation before retry; retry before stale/terminal guard; recover bytes even after completion. Changed stale commands fail. Opening clock loss stores no fabricated Timing, queued owner may only trigger System fallback.
- Release preserves spent CP. I ceiling CPA; II floor(CPA/2); existing overspend never refunded. CPA9/spent3/II permits only1 voluntary CP; spent7/ceiling5 remains7. Release preserves offensive consumption and validates link to original attacker commitment; it does not itself consume allowance. Actual released-Reserve assault remains outside selected profile.
- Pending next-Movement exception retains same scope and ordinal+1, waives only proximity, executes no Movement and expires only through018/019 semantics. Preserve expired receipt/history. Checked ordinal/version/capacity failures do not become retention/fallback.
- Derive `rel.` identity from baseHash/cycleId/position; `rr.` receipt from canonical unsigned event, then link conversion/release history to receipt. Original input actor retained even when clock fault makes event System-authored. No self-hashing. Extend prefix once, retain immutable owned arrays/bytes/history and exact readback via independently reconstructed base plus suffix. Complete stays at same Reserve Release position.
- World projection changes only matched original own reserveStatus; full membership equality, CPA/spent invariance and initial World hash must be independently proved. No CP/ammo/TOE/Cohesion/location/RNG/relationship/custody/future-obligation mutation. Progress only I→none/I→II/II→none with exact event reference; retain-II/open/complete not progress.
- Canonical bytes1MiB, depth32, arrays512, members32, events34; raw malformed frames before context. Do not reuse Result2's32-event cap for Release's34-event suffix.

## Proposed dependency-valid packets

All names below provisional; root records selected child IDs/ownership before implementation. Five-file counts include focused tests and plan update; no implicit maintenance exemption.

| Packet | Exact likely primary paths | Acceptance and limit |
| --- | --- | --- |
| 017A native kernel and full Release history | New `src/Cna.Core/Campaigns/CampaignCombatReserveRelease.cs` (nested owned models + kernel); new `CampaignCombatReserveReleaseCodec.cs`; new `tests/Cna.Core.Tests/Campaigns/CombatReserveReleaseTests.cs`; existing `tests/Cna.Core.Tests/Cna.Core.Tests.csproj`; canonical plan | Five paths. Private isolated/trusted-boundary semantics first/later/empty and strict history/replay. No actual positive campaign admission; no parent017 close. If model size requires separate file, split first or explicitly re-scope, not silent sixth path. |
| 017B native empty Release from accepted016 | New `src/Cna.Core/Campaigns/CampaignCombatResultRelease.cs`; new `tests/Cna.Core.Tests/Campaigns/CombatResultReleaseTests.cs`; csproj; canonical plan; at most one narrowly necessary kernel/codec change | Four–five paths. Replays full authenticated Result2 tuple to closed source then native open/complete, exact64 native event literals; synthetic boundary provenance retained. No CycleControl implementation merely to compare wrapper hashes. |
| Prerequisite positive3h family | Separately bounded owner-named inherited Reserve-cycle packet(s), after existing actual designation/opening | Missing direct no-move completion, idle Breakdown, held-I empty selection/no-attack cumulative replay. Likely crosses >five existing modules if extending ordinary route; freeze dedicated model/kernel+codec+tests+fixture link+plan packet or split by actual event family before editing. Exact path manifest requires targeted follow-up; this research does not authorize or pretend scope already fits. |
| 017C positive3i Release adapter | New `src/Cna.Core/Campaigns/CampaignCombatInheritedReserveRelease.cs`; new `CampaignCombatInheritedReserveReleaseCodec.cs`; new `tests/Cna.Core.Tests/Campaigns/CombatInheritedReserveReleaseTests.cs`; csproj; canonical plan | Five paths only after accepted3h runtime predecessor and017A. Actual both-owner release-I three-event sequence with status-only World projection and full predecessor replay. No later-II/alternate-conversion runtime claim. |

Administrative worker/acceptance/review evidence is additional physical artifact count, always enumerated. Parent017 status must identify what kernel versus reachable admission evidence satisfies each canonical criterion; unmet positive/later provenance remains explicitly open. A+B may proceed after016 acceptance with no fake positive source. If canonical owner interprets017 as demanding all first/later behavior from reachable full campaign histories at once, missing prerequisite work blocks parent closure, not bounded kernel research/implementation.

## JSON-only inventory (not executed C# or oracle evidence)

Parsed existing fixtures using `python3 -B` standard-library `json`/`hashlib`, with no imports from oracles and no writes. Counts independently reproduced:

| Fixture | Retained evidence | Meaning |
| --- | --- | --- |
| `combat-reserve-release-v1.json` |13 cases;48 side/slot goldens;48 base hashes;188 state-cut hashes+lengths;140 event hashes;3 literal terminal events | Not140 event literals or188 full-state literals.48 splits26 first-slot/22 second-slot; settled guard/escape are first-slot only. |
| `combat-inherited-reserve-cycle-v1.json` |2 owner cases;23 hashes+lengths each =46 | Per owner:1 predecessor-record-digest-list,1 base,10 event hashes,11 Control-state hashes. Totals20 event hashes/22 state hashes; no full canonical event/state literals. |
| `combat-inherited-reserve-release-v1.json` |2 owner cases;2 predecessor hashes;2 base hashes;8 Control frames;6 event hashes;2 literal terminal events | Not6 event literals. First-I release-I only,4 cuts per owner. |
| `combat-result-cycle-finish-v1.json` |32 traces;128 full native event strings inside wrapper objects;160 wrapper-state cut hashes;32 native ReleaseBase strings | Whole bridge includes64 Release and64 Cycle events.017 prefix owns64 exact native Release event strings,32 bases and96 wrapper-state hashes at first3 cuts. Wrapper hashes are not native Release-state hashes. No retained full native Release-state JSON literal. |

Bridge prefix assertions reproduced: every trace begins native reserve-release-opened then reserve-release-completed; System actor, null ReleaseBase high-water, singleton status-none member, empty opening pending queue and null timing. Source14 pins retained. Full Result2 antecedent inventory from016 research remains272 event literals/304 state hashes/32 final JSON literals. Appending64 native Release events gives336 cumulative event literals;368 unified source+native event-boundary cuts is arithmetic target (304+64), **not368 existing matching native state-hash vectors**. Bridge retains separate wrapper-state hashes; do not add hash domains as if interchangeable. Discard/replay at each actual native cut can be tested without falsely claiming frozen native final literals.

Fixture SHA256 pins:

- Release: `70ed683c21f95a511c06362765dbe0971e0bb024bfcee74be70865be70601af9`.
-3h: `22d349c63acd9f567c9691f71bc5ea007334e596b5bf9f4789e1a6c229f7e10e`.
-3i: `56479736788241186bce083b915aa5b4912fd6f745e9080e3707a166febfe3d0`.
- Result2 bridge: `a1b6cb5f611586da62667fd4315365aa3a38c34af8afe016314eba4c7c1057c2`.

Recount recipe: sum `goldens[*].frames/eventHashes`, count present `terminalEvent`; for3h interpret oracle `goldens` order above; for bridge parse `traces[*].events[*].nativeEvent`, inspect first2 native kinds and count `cuts[:3]`. JSON shape exploration initially treated wrapper object as string and failed; corrected to `nativeEvent` field before reported counts. No runtime verification inferred.

## RED, evidence and next gate

Meaningful017A RED: frozen first-I owner release/convert sequence absent, then first fallback drain, later bulk retain and consumed-convert retry/control must fail before implementation; missing API compile RED alone is insufficient final behavioral evidence. Match48 base hashes/140 event hashes/188 state hashes+lengths and3 terminal literals with independent literal expectations, not regenerated goldens. Test all retry/callback/ownership/canonical bounds, offensive consumption/CPA/exception/history forgeries and frozen source pins. Separate fake probe base construction in tests from production admission; a public constructor accepting arbitrary digest is not proof.

Meaningful017B RED: accepted016 full closed state plus next exact native release-open command cannot be emitted before adapter; then exact second completion. Verify both owners/all32 source contexts, all native cuts, no clock inherited from private result acceptance, byte/resource/history/future-obligation preservation, raw/typed tampering, old retries after completion and no auto cycle progress. Existing CombatResolution/LossRetreat/Custody/Closure goldens unchanged. Existing ordinary Reserve designation/opening tests unchanged. No new fixture generation needed for native event parity; any demand for retained native full-state goldens requires separately authorized evidence work, not inventing counts.

Meaningful3h prerequisite RED: replay actual held-I opening plus frozen first direct no-move completion currently rejected.3i RED only after accepted3h terminal, then missing first native Release open. Both need independent full predecessor/cross-history checks; supplied terminal World/hash cannot substitute.3h/3i hash vectors can be checked from C# against retained hashes without pretending full event literals exist.

Confidence high for missing API gates, frozen behavior and inventory; file layout/016 integration provisional. Unknowns: Task015 independent acceptance outcome; final accepted016 state/receipt APIs; exact bounded3h implementation split; actual later-II/consumed campaign lineage availability after019; root's parent017 completion interpretation. No contract change needed for bounded native kernel/empty adapter; positive predecessor is implementation gap requiring explicit planning, not new policy or storage spike.

Next gate: finish015 acceptance, implement/accept016, then re-read frozen016 APIs and root approves concrete017A manifest plus parent deferrals. Native017B followsA and016;3i waits for explicit accepted3h predecessor. No implementation authorized by this research. Actual positive history, cumulative HistoryReplay/Snapshot extension, all28 runtime traces, public activation/HOST-PUB-001 and final Task025 acceptance remain open unless separately proved.
