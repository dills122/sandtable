# Author Explanation — Task017A1 isolated Release bases

## Intent And Success Criteria

Add immutable typed Release scope/history/exception/member/base values and canonical isolated ReleaseBase codec. Match44 independently constructed frozen base hashes, preserving strict raw/context/semantic validation and ownership. A1 supplies a bounded foundation for native Release lifecycle; it does not execute Release or close parent017.

## Plan-To-Implementation Traceability

Canonical Task017 refinement splits A1basecodec, A2native lifecycle, Bnative empty adapter and separately certified positive predecessor/bridge. A1 owns four source/test paths plus root-owned plan. Frozen schema/fixture/oracle unchanged. Exactly44 isolated rows included; four historical settled Result1 rows explicitly excluded. Implementation base afdcd9e. Candidate freeze follows completed development review and worker handoff; full gates/reviews pending.

## Technical Approach And Flow

Caller supplies independently retained creation request and independently expected typed base. Serializer validates base semantics/context, writes exact frozen field order, then checks primitive grammar and frame bounds. Reader rejects malformed/noncanonical raw bytes before trusted inputs, validates independently expected typed base, compares complete canonical bytes, then returns immutable owned expected value. No incoming JSON-derived expected authority or hash-only authentication. Internal isolated profile explicitly excludes real World provenance.

## Changed-Component Walkthrough

Models contain Release-owned immutable values and copied read-only member/attack collections; existing cycle, UnitKey, CP and RNG types remain unchanged. Codec owns explicit base/nested writers and closed raw shapes, expected-byte comparison, cycle/creation/topology binding, Reserve/history rules and offensive-link matching. Tests construct typed probe bases from retained Created11/Content7/Result2 template and frozen recipe without expected hashes as inputs. Existing Core test project links frozen Release fixture. Root plan/status/evidence are administrative changes.

## Decisions And Rejected Alternatives

Do not import old Result1 occurrence IDs or weaken World7 to claim four historical settled rows. Do not implement lifecycle or generic schema engine inside this slice. Reuse immutable domain primitives and existing cycle writer; keep Release-specific history separate from designation-only history. Returning independently expected immutable base is safe because constructor copies collections and nested records contain immutable values; no raw JSON authority retained.

## Invariants And Boundary Conditions

Full creation/setup/content/rules/config identity matches retained request. Official same-turn/stage/relative-slot Release position and actual acting side required. Members strictly sorted/unique, own original side and creation binding; CPA positive, CP reduced/nonnegative but historical overspend preserved. ReserveI only first ordinal, II only later; designation/conversion/released history and expired next-Movement exception remain coherent. Offensive commitment resolves exactly once to matching original attacker and turn/stage. Raw closed fields, canonical spelling, integer/ID/hash/UTC bounds, frame/depth/array limits precede trusted access. RNG/prefix/receipt/World fields must equal complete independently expected bytes; hashes alone convey no authority.

## Verification Performed And Results

Final post-format worker focused/shared20passed (six Release facts plus seals/custody), zero failed/skipped26.284s; scoped format/diff passed and four pins retained. Root unchanged Release oracle passed48historical traces; that does not expand C#A1 beyond44basehashes. Full root gates and independent reviews pending. Meaningful behavioral RED independently inspected: compiling skeleton produced empty-object hash instead of frozen first-release Axis/first-slot base hash. Root development review found raw canonical-ordering gap and AttackHistory cycleId grammar gap; both corrected before candidate freeze. Attack-cycle regression separately reproduced missing hash rejection; raw canonical cases verify null trusted inputs remain untouched. Final counts, commands, failures and pins belong worker/check records.

## Risks, Tradeoffs, And Maintenance Costs

Expected base is explicit caller trust input. Supplying candidate-derived expected value defeats authentication; API documentation states this. Dormant internal codec admits isolated-ledger probes only, so it does not authenticate actual retained World or campaign history. Specific nested shape checks duplicate frozen field inventory intentionally; golden hashes protect drift. No transport/public API or persistence schema migration.

## Deviations, Deferrals, And Known Gaps

Zero lifecycle/event/state transition proof.132 event hashes/176 state hashes/2 terminal event literals belong laterA2;64 native empty bridge events belongB. Positive held-I/no-move3h runtime precursor,3i bridge and later-II/consumed actual lineage remain open. No Task018/019, Snapshot successor, durable publication, future-duty execution or public activation claim. Four source/test paths remain within manifest. Root full gates and three independent reviews remain required before acceptance.

## Challenge Points For The Reviewer

Review tests/plan/source blind before testimony. Verify raw canonical rejection precedes trusted expected/context, complete expected-byte equality, inherited AttackHistory grammar, cycle/creation bindings, legal history edge cases, real ownership, exact44/4 inventory and absence of fixture-backed production state. Distinguish base hash parity from later event/state and actual campaign proof.
