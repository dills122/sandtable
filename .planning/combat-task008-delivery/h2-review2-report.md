# H2 independent review

Review instance: 2 of 3.

## Blind preliminary ledger

Recorded before author/check packets or session recall. Frozen four primary hashes verified against h2-source.sha256; HEAD64cd01107ca249809377cf246a17893bda2dcd35, branch codex/combat-task008-reaction-lifecycle. New untracked CombatMovementHistoryReplayTests.cs inspected explicitly.

- No concrete correctness finding identified. Reserve partition ends only on accepted Completion. Movement tag merely chooses strict reader; exact regenerated bytes remain authority check. Three lifecycle slots are admitted by reader requiring actual moves and exact stop/resolve/complete command sequence. G2 reader requires completed idle lifecycle/end proof and allows at most one event; unsupported suffixes reject.
- Tests compare actual reader state bytes and frozen family event/state hashes at each selected cut, plus H0 typed fields. Both owners and move counts1/5/6/7 cover expected unique fixture histories. Forged canonical typed effects and actors supplement raw malformed mutations. Reaction same-tag tail rejects via ordinary strict reader.
- Buffer ownership inherited unchanged from bounded Capture; new projection tests mutate input and returned copies, then replay owned retained bytes.
- H2 remains scoped router work. Plan explicitly leaves Reaction H3, literal-root/fresh-admission-disabled H4, publication and later gameplay open. Full root byte equality is not claimed by current H2 tests.
- Potential concern checked and dismissed: lifecycle partition uses count3, but each member is exact causal replay; count does not confer validity. Movement initialization cannot admit designated/non-Normal opening; this matches inherited ordinary reader scope.
- Remaining verification: reconcile author claims and completed test evidence; independently inspect fixture identity inventory. No .NET process launched because root cumulative suite owns execution.

## Findings

No actionable findings. Review covers four frozen primary paths, including untracked new test, relative to64cd011. No prior review report or aggregate verdict evidence read before verdict. Execution ledger was checked by H2/Initial-H-specific excerpts. Targeted session recall was subsequently completed after blind ledger and initial report, as disclosed below.

## Plan Review

Implementation satisfies bounded008H2 objective: one trusted Created11/request/event stream produces ordinary Movement, mandatory stop/resolution/completion, then Breakdown completion/Combat entry. H1 short prefixes return before ordinary-profile initialization, preserving designated and non-Normal opening prefixes. Optional designation at event10 cannot stand in for completed opening. Readers continue reconstructing exact causal effects from trusted predecessor authority; eventType does not authorize a state transition.

Scope remains within four primary files. README.md, tech-design.md, naming-overview.md and docs/design/combat-cycle-implementation-plan.md keep H2 in progress and distinguish router projections from full Snapshot12 roots. H3 Reaction, H4 full-root byte parity and fresh-admission-disabled restore, HOST-PUB-001 persistence/publication, positive vehicles and later gameplay remain explicitly open. No architectural pivot, schema migration, operational rollout or new service introduced. Dev and three independent rounds remain delivery gates owned by lead.

## Author-Claim Reconciliation

| Claim | Evidence inspected | Status | Consequence |
| --- | --- | --- | --- |
| Accepted typed Reserve completion establishes Movement boundary | CampaignCombatHistoryReplay.Replay reserve loop; reserve designation negative test; unchanged ordinary Initial guards | Confirmed | Count10 does not incorrectly confer authority. |
| Exact strict readers reject same-tag Reaction and untrusted effects | IsMovement dispatch; each Replay byte comparison and Emit/Command guards; real F1 negative and canonical forgery tests | Confirmed | Tag is dispatch hint; committed-history authentication still external. |
| Mandatory lifecycle and G2 gates cannot be skipped/truncated internally | Lifecycle Replay/Command/Emit; G2 Replay preconditions/count; every-occurrence omission/reorder/foreign-tail tests | Confirmed | Valid shorter prefixes stay valid; missing trusted terminal head is not detectable by this seam alone. |
| 48 selected histories/64 H0 vectors covered | Independent JSON identity inventory; AssignedFrozenFamiliesHaveExactExhaustiveHistoryIdentityCoverage; every-prefix tests | Confirmed | Both owners and route lengths1/5/6/7 cover selected family inventory; no claim of unrestricted campaign histories. |
| Canonical forged effects are stronger than malformed scalar negatives | CanonicallyResignedTypedEffectsAndAuthorityRejectAgainstActualPredecessors checks changed receipts, readable input and router rejection | Confirmed by source | Covers cost, actor, interrupt cycle, restored position, end locations and G2 successor. |
| Buffers remain owned and projections typed | Unchanged bounded Capture; three sealed projection arms; input/export mutation and replay test | Confirmed | No caller family cache or mutable byte alias added. |
| Focused70 pass; full build clean | /tmp/h2-green-final.log; /tmp/h2-build.log | Confirmed from retained execution logs | Reviewer did not execute these .NET checks independently. |
| Full Snapshot12 and disabled-admission restore remain future work | Author packet, implementation plan and current serializer assertions | Confirmed | H2 readiness cannot close parent008H. |

## Verification Performed

- `shasum -a 256 -c .planning/combat-task008-delivery/h2-source.sha256`: four OK, performed twice before/after inspection.
- `git branch --show-current`, `git rev-parse HEAD`: expected branch and base64cd01107ca249809377cf246a17893bda2dcd35.
- Scoped `git status --short`: three modified tracked paths and new untracked CombatMovementHistoryReplayTests.cs; scoped diff inspected.
- `git diff --check 64cd011 -- src/Cna.Core/Campaigns/CampaignCombatHistoryReplay.cs src/Cna.Core/Campaigns/CampaignCombatHistoryModels.cs tests/Cna.Core.Tests/Campaigns/CombatHistoryReplayTests.cs`: exit0. Does not include untracked file; new test source inspected directly.
- Read-only `python3 -B` fixture audit: E1=16, E2G1=32, G2=16 vectors;64 total,48 unique Created/hash-sequence identities; lengths10–21; SHA256 of all64 canonicalJson byte strings matches frozen hashes. Exit0.
- Retained focused execution log: `dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --filter-class '*HistoryReplayTests' '-bl:/tmp/h2-focused-{}.binlog'`,70 passed/0failed/0skipped,31.386s. Log inspected, command not rerun.
- Retained build log: build succeeded,0warnings/0errors,3.88s. Reported full-format exit0 in author checks is not independently re-executed.
- Root cumulative suite/boundary execution remains root-owned; no concurrent .NET execution or TRX writes by reviewer. No Python oracle rerun, fixtures or implementation changed, Git mutation, delegation, or extra review round.

## Open Questions And Residual Risks

Repeated prefix replay has bounded quadratic cost. Current ordinary profile supports at most32 moves and selected legal traces reach7; no evidence of practical regression at selected scope. Larger retained profiles should revisit composition cost when introduced.

H0 field comparisons plus family goldens establish typed state equivalence, not full literal root byte equality. Missing terminal committed history is indistinguishable from a legitimate shorter prefix without external trusted head. These are explicit H4/publication obligations, not H2 defects.

## Verdict

Ready for bounded H2 slice, subject to lead completing remaining independent round and cumulative delivery gates. No blocking or non-blocking code finding identified.

## Recommended Next Actions

Retain frozen target through final review and cumulative gate reconciliation; then record accepted H2 evidence. Continue H3/H4 under existing plan without promoting H2 into full restore/publication completion.

All reviewer-launched processes completed. No background process or delegated task remains.

## Post-ledger memory/process disclosure

Within same review instance, lead requested required targeted recall after preliminary ledger and source verdict. Called `session_recall("H2 Movement Breakdown retained-history routing causal partition implementation")`. Tool returned broad historical matches despite targeted query, including unrelated BRK verdict summaries plus relevant H1 bounded-owned-history decision, G2 actual-lifecycle prerequisite, and E2/G1 exclusion-predicate rationale. No linked timeline or prior report opened. Returned summaries treated only as context, not readiness evidence; no H2-specific contradiction or new finding identified. Initial source-based verdict unchanged.

Lead subsequently reports cumulative solution suite2174passed/0failed/0skipped in5m22s229; boundary check next. This is lead-reported evidence, not reviewer-executed verification. All reviewer processes remain completed; no new review round or source edit performed.
