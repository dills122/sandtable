# F6 completion after second move dispatch

Start only after previous delivery slice accepted by lead (dev+three independent reviews).
Read f6-scope.md and its exact canonical spec/schema/fixture/oracle. Actual causal predecessor from
scope, not prior delivery slice terminal unless specified. Internal worker, shared repo, preserve others.
Own exactly five primary files: CampaignCombatReactionCompletionModels.cs,
CampaignCombatReactionCompletion.cs, CampaignCombatReactionCompletionCodec.cs under src/Cna.Core/Campaigns;
CombatReactionCompletionTests.cs under tests/Cna.Core.Tests/Campaigns; testcsproj fixture link only.
No accepted predecessor edits/canonical changes/docs/git/PR/delegation. Report before scope expansion.

TDD exact every-owner golden input/event/state/cut bytes and literal invariants before implementation,
then identity/preimage/receipt/capability independently, every actor/retry/fork/history and raw/re-signed
field/cache mutations, typed wholeWorld guard, ownership buffers and capacity. Preserve predecessor
reader guards; use actual trusted history, no synthetic Snapshot11/cached-state admission.
Focused new tests plus immediate predecessor compatibility; scoped format. Native MTP exact --project
and --filter-class; unique /tmp/f6-*.binlog/log and approved MSBuild IPC escalation. Root owns full
build/format/boundary/suite and review orchestration; avoid concurrent root builds.
Existing baseline /tmp/f6-reaction-baseline.log passed; verify pins/source, rerun only if concern.

Return frozen scope/source manifest, exact test commands/results/logs including earlier failures,
choices/costs/deviations/limitations/questions and no active processes. Lead integrates and performs
three sequential independent review rounds before acceptance. No parent/H/publication completion claim.

Test-harness lessons retained from F3/F4: assert exact fixture case counts before filtered loops;
positively ReadState-roundtrip initial cut and every successor, not only mutate initial cache.
Shape-valid changed source atoms can deserialize legitimately: test rejection through authoritative
Apply(DeserializeInput(mutated)), while raw noncanonical/invalid-shape cases target decoder alone.
Compare reconstructed route/track collections structurally rather than reference equality.
Use valid opposite-owner retained history for foreign-history tests; arbitrary RNG seed may fail
an earlier bounded Weather gate before reaching intended assertion. Keep such setup failures explicit.
