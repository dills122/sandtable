# F5 second Reaction move research

Execute after F4 review; causal predecessor is F2 exactly first participant move at14, not F4 terminal.
Canonical combat-inherited-reaction-second-move-v1 spec/schema/fixture/oracle. Two owner traces,
two events/four cuts/10 artifacts. Actual full creation/E1/F1/F2firstmove history, active sole opportunity,
reactor at rear CP2, one legal Clear2 rear-to-supply move. Actor reacting owner only.

Command2 stable public window (trigger13), opportunity rotates at14 using complete single-option
capability, actionv1 exact kind/handles/origin/destination/cost. Public handles differ from persisted IDs.
Move3 event14→15 retains compatible F2 event order and irl receipt domain. World element and
representation move together; CP2→4, Cohesion0, no RNG or lots. Existing track appends supply;
route ID/start14/assault origin/owner/empty cohorts retained, only current location changes.
Active window and opportunity stay open; one progress reference added. Phasing CP4/route unchanged.
No completion/closure/stop or third move. Exact retry uses original event/current terminal.

Expected five primary files new SecondMove models/projector/codec/tests plus fixture registration.
F2 firstmove wrapper and whole-World derivation reusable in concept; F2 command generator intentionally
chooses completion at this cut, so do not broaden its guard. Own second-move adapter, reusing pure
identity/action/route/spending helpers. Must guard new full typed World rather than silently discard
unsupported fields. Inspect actual F2 interfaces before coding. All goldens, independent public/preimage,
literal track/route/resource preservation, actor-before-retry, forged/re-signed every leaf/cache/history,
first-step handle/wrong step, missing/extra events, buffers/byte/depth/array capacity tests.

Baseline passed: /tmp/f5-reaction-baseline.log;2traces2events4cuts2retries260mutations60raw48boundaries14pins.
Command PYTHONDONTWRITEBYTECODE=1 python3 docs/specs/verify-combat-inherited-reaction-second-move-v1.py.
No public activation, vehicles/multiple opportunities, broad Snapshot12 or publication claims.

Verified interface note: F2.MoveOptions is internal and exposes actual one rear→supply option at14;
F2.Command intentionally chooses Complete at that cut, and ProjectMove is private. Use existing pure
CampaignObservationV6DisclosureIdentity capability/opportunity helpers and move action candidate to
construct F5's separate actual second-move command. Do not fabricate a mutated F2 state to satisfy
its writer: F2.WriteWorld checks ExpectedWorld for its own bounded event history. F5 needs its own
history-derived whole-World guard/writer for CP4/supply. Existing route retains identity/start14;
append supply to existing reactor track, not a second track. F6 can delegate to accepted F5 guard
because its three terminal transitions leave World unchanged.
