# H2 independent review

Review instance: 1 of 3.

## Preliminary blind ledger

Recorded before author explanation, checks packet, session memory, or prior review reports.
Frozen four primary hashes match; HEAD 64cd01107ca249809377cf246a17893bda2dcd35 and branch codex/combat-task008-reaction-lifecycle match bootstrap. New untracked CombatMovementHistoryReplayTests.cs explicitly inspected.

No actionable defect found in initial source/test/plan pass. Router validates every prefix using existing exact-byte causal readers. Accepted typed Reserve completion gates movement; element-moved only selects candidate reader; mandatory three lifecycle records and final completion preserve existing strict effects validation. Extra tails reject through completion reader. Earlier H1 return boundaries preserved.

Questions to reconcile: exact fixture-history coverage (claimed 48 unique histories), focused test execution evidence, retained history copy/capacity guarantees, supporting docs and active H2 ledger consistency. H3, H4 and publication excluded by canonical plan and bootstrap; no full-root or disabled-admission proof inferred.

## Findings

No actionable findings in frozen H2 change. Read-only review; no implementation, fixture, or Git mutations.

## Plan Review

H2 satisfies bounded E1 → E2/G1 → G2 routing scope using unchanged causal family readers. Four primary files fit bounded child scope. H1 prefix handling survives: return before ordinary-profile initialization preserves valid completed storm/designated Reserve histories. Typed completion, not event count, establishes ordinary movement partition. Lifecycle reader validates exact stop/resolution/completion order, then Breakdown reader rejects more than one remaining event and recomputes Combat-entry effects. Internal-only closed projections retain complete predecessor authority without separate caches.

README, tech-design, naming-overview and canonical execution index accurately describe H2 as active and leave H3 Reaction, H4 literal root/admission-disabled restore, later gameplay and HOST-PUB-001 open. Execution ledger records active dispatch without claiming acceptance. Acceptance bookkeeping and remaining required independent rounds belong to lead.

## Author-Claim Reconciliation

| Claim | Evidence | Status / consequence |
| --- | --- | --- |
| Single stream derives causal boundaries | HistoryReplay.Replay; ReserveOpening, InheritedMovement, MovementLifecycle, BreakdownCompletion readers | Confirmed; all bytes pass exact recomputed event comparison |
| Shared Move4 Reaction rejects | SameTagActualReactionTailRejectsThroughStrictOrdinaryEffects; unchanged ordinary Emit constraints | Confirmed by source and retained passing focused log |
| 48 selected histories / 64 vectors | Independent Python audit of frozen H0 roots; test exhaustive history set equality | Confirmed; E1 16, E2G1 32, G2 16 vectors; 48 unique identities |
| Actual states/events match frozen predecessor evidence | EveryOrdinaryLifecycleAndCombatEntryPrefixMatchesActualReadersAndFrozenEvidence; CheckH0/CheckGolden | Confirmed; full family bytes plus H0 authoritative field comparison; no literal full-root-byte claim |
| Typed canonical forgeries reject | CanonicallyResignedTypedEffectsAndAuthorityRejectAgainstActualPredecessors | Confirmed source sensitivity: input decodes, receipt changes, router rejection required |
| Owned bounded retained buffers | CampaignCombatRetainedHistory.Capture and copying getters; CallerAndResultBuffersCannotMutateAnyNewTypedProjection | Confirmed; unchanged limits before payload copying |
| Focused 70 pass, build pass | /tmp/h2-green-final.log and /tmp/h2-build.log | Confirmed retained outputs, not reviewer-executed .NET |
| Full cumulative gate | Root coordination only at review time | Unverified here; lead owns completion |

## Verification Performed

- `shasum -a 256 -c .planning/combat-task008-delivery/h2-source.sha256`: four hashes OK before and after review.
- `git rev-parse HEAD`, `git branch --show-current`, scoped `git status --short`: exact target confirmed, including untracked new test.
- Scoped `git diff HEAD` and `git diff --check HEAD`: reviewed production/test/doc changes; whitespace check exit 0.
- Read-only `python3 -B` fixture audit: selected E1/E2G1/G2 root ASCII byte lengths and SHA-256 values all match; each root's full receipt event-hash ledger matches trusted event-hash array; stateVersion equals event count plus one. 64 vectors / 48 distinct histories verified.
- Retained focused command: `dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --filter-class '*HistoryReplayTests' '-bl:/tmp/h2-focused-{}.binlog'`; log reports 70 succeeded, 0 failed, 0 skipped, 31.386s. Build log reports 0 errors and 3.88s. No .NET process started by reviewer, per coordination request.
- Session recall performed only after preliminary ledger and source/spec review; canonical files remain controlling evidence. No prior review report read.

## Open Questions And Residual Risks

Repeated prefix reconstruction costs grow quadratically in accepted move count; existing 32-move and bounded history limits contain current dormant profile. Full-root snapshot byte validation and independent committed-head binding are intentionally not exposed by this router. No deployment/public admission path changes. Full cumulative gate must complete before lead acceptance. Reviewer processes all finished.

## Verdict

Ready for bounded H2 scope. No blocker or non-blocking defect identified. This verdict does not close parent Task008, waive remaining scheduled independent rounds, or establish full restore/publication evidence.

## Recommended Next Actions

Lead completes cumulative gate and remaining sequential reviews, then updates H2 acceptance evidence. Preserve H3/H4/publication exclusions.
