# Author Explanation — D1 Reserve designation

## Intent and plan
Task008 D1 supplies complete state10 predecessor and optional own none→I designation. First causal
World mutation on this path, with exact precompletion private ReserveState1. Full D scoped into
D1 designation→D2 completioncodec→019A atomic terminalprojection, then E. Frozen completion2 already
carries cycleidentity;019A must apply same event, never add another command/event. Full terminal
Reserve/retryaftercompletion/Movement remains pending. This is an execution split within original
fiveprimaryfile requirement, not a changed wire contract or dropped acceptance condition.

## Flow and components
CampaignCombatReserveModels owns command/input/event, typed owner scope/designation-only history,
member and immutable private state. ReserveDesignation.Replay requires completeB2 fourstageevents,
which revalidate actual creation/opening/Weather, then resolves owner from retained firstsideorder and
Content element ownership. Initial state has nine receipts/one ownmember. Emit accepts only exact
ownercommand atstate10, creates rd.receipt, reconstructs typedWorld changing ownreserveStatus only,
and appends actualreceipt into member history and command ledger. Apply authorizes before retry;
ReadState compares entire independently replayed projection. No supplied cache/prefix certifies state.
ReserveCodec owns canonical input/event/state and boundedWorld writing. ExpectedWorld reconstructs
initial or designated World from predecessor and acceptedinput; equalityguard protects hardcoded
absent fields in serializer. InitialWorldcodec untouched. Tests plus fixturelink cover all16rows
but only24precompletioncuts; no claim for frozen terminal16cuts.

## Choices and invariants
Typed World reconstruction preserves every immutable element and world property except accepted own
none→I. No JSON-domain mutation or genericrestore. Bounded writer duplicates current closed-profile
field layout to avoid widening initial-only codec; futureWorld profiles require separate validation.
Memberhistory retains actualdesignationreceipt and exactscope; futureconversion/release/attack fields
canonicallynull. Bothownerorders/allweathertypes preserved, nullReserveActiveSide remains. CP0/ammo10/
TOE/location/representations/RNG unchanged. Inputbuffers copied and retainedstate read-only. Counts
bounded to full4/1/4predecessor plus0–1designation,1MiBrecords/depth32. Completiontag rejects here.

## Verification and dev review
Exact executedresults in d1-evidence.md. Frozenrequest/creation/predecessorlist and precompletion
state/input/event fingerprints; all16rows,24cuts,8designationevents. Actor/opponent/System/unknownmember,
changedretry, malformed/foreignhistory, canonicalraw/leaf and re-signedforgery/cache, bufferownership,
initialreader/oldreader rejection covered. Lead requested decoded valid-ID mutations be checked at
Apply instead of assuming syntacticinvalidity; additionalbounds/completiontag and typedforgedWorld
writer probes included beforefreeze. First runtime parity check caught legacy reserve-i versus frozen I spelling; fixed only bounded
Reserve writer with closed none/I formatter, leaving legacy serialization unchanged. Source review
found no remaining actionable issue. Oraclebaseline covers
entirefutureReservecontract; distinguish that from D1implemented subset.

## Costs, limits and challenge points
BoundedWorldwriter layout duplication is intentional scope tradeoff; exactfixtures and equalityguard
prevent unchecked drift or broaderstateadmission. Models expose onlydesignationhistory fields supported
here; laterowners extend typedshape without silently activatingfuturebehavior. No terminalcycle,
completionprojection, release, genericSnapshot12, publicingress or durablepublication. Inspect owner
resolution, preservationofopposingstate, typedWorldguard, actualreceipthistory, codecstrictness and
D1/D2/019A dependencyrefinement. Neither checksums nor trustedactormetadata authenticate remoteinputs.
