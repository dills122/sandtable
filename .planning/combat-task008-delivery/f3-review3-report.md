# F3 independent review

Review instance: 3 of 3. Target: explicit working tree over c83d7c4; branch codex/combat-task008-reaction-lifecycle. Five primary source hashes match f3-source.sha256. Review read-only except this report.

## Preliminary blind ledger

Recorded before opening f3-author.md, f3-evidence.md, or session recall. Bootstrap/parent disclosed desired scope and aggregate passing checks; no author rationale or earlier reports read. Narrow CCE code-symbol searches returned only code/tests, no author packet.

- No actionable defect identified in first pass through all three new implementation files, new tests, canonical closure prose/schema and oracle transition, predecessor Replay/Initial, WindowCapability and WriteWorld, and supporting documentation diffs.
- Exact replay starts at creation and always passes empty participant-lifecycle history; F1 inactive guard enforces version13, one opportunity, resume-route and no reactor route. Emit compares complete generated reason-specific input; retries compare complete accepted input after authorization.
- Canonical event field order and receipt domain match frozen oracle; regenerated byte equality rejects effect/input/cache forgeries and noncanonical payloads. State preserves predecessor World/tracks/progress/receipts while advancing one version/prefix/receipt and clearing interrupt/window.
- Test coverage directly reconstructs both-owner six forks, 30 golden artifacts/12 cuts, public IDs/actors, competing forks, re-signed every-leaf effects, history corruption, buffer detachment and whole-World guard. Need reconcile retained execution evidence, source pins and fixture counts before verdict.
- Plan retains F4/F5/F6, public activation, parent restore and scheduling as open; bounded F3 scope justified. Documentation remains in-progress pending acceptance, not prematurely claiming completion.
- Residual concern to assess: inherited writer coupling and hard-coded version13 are intentional exact-profile constraints, not broad restore APIs. No evidence of new runtime registration.

## Findings

No actionable findings. No P0–P3 defect demonstrated in frozen F3 code or bounded implementation plan.

Scope verified at HEAD c83d7c4e18ddb03692deec303c66621da839f2f8, with four untracked F3 implementation/test files plus changed test project; tracked navigation/execution edits are supporting scope. `git diff` alone excludes new files, so all four were explicitly read. No old F1/F2 source modification, generated artifact churn, runtime registration, or contract mutation found.

## Plan Review

Task008 execution index gives F3 actual F1 predecessor and reserves F4–F6 for active fallback/second movement/completion. Implementation honors that boundary by replaying lifecycle with an empty suffix, then allowing only one direct-close event. No caller cache replaces creation-rooted evidence. Reuse of established action IDs, window identity and whole-World guard is appropriate; duplicated canonical state/event writing avoids widening predecessor contracts during this small slice.

Six reason/owner branches cover specified acceptance gate: two possible phasing owners × reacting-owner decline or System unavailable/timeout. Each advances 13→14, appends exactly one receipt/prefix, restores exact route, removes interrupt/window, and preserves material state. Tests independently derive public action/window, receipt and prefix identities and compare all unchanged state properties plus route. Native public event/Snapshot12 readers still reject these dormant records.

Supporting README, design plan, roadmap, naming and technical rationale agree that F3 is in progress pending acceptance. Lead may update acceptance bookkeeping after this report. Parent restore, F4–F6, scheduling, activation and actual publication remain open; no rollout/migration promise follows from this internal codec gate. No architectural pivot needed.

## Author-Claim Reconciliation

| Claim | Evidence inspected | Status | Consequence |
| --- | --- | --- | --- |
| Actual F1 history only; no completed/active F2 admission | Closure.Replay; Lifecycle.Replay/Initial; ActiveLifecycleForeignAndAlteredActualHistoryReject | Confirmed | Exact trigger authority reconstructed from creation before closure. |
| Authenticated actor checked before exact retry | Closure.Apply/Authorize/Actor; ReasonForksPublicAuthorityAndActorsRejectBeforeAndAfterRetry | Confirmed | Wrong actors, crossed IDs, changed prior version and competing reasons reject before successful retry. |
| Close3 retains 24 ordered fields and irl receipt | ClosureCodec.SerializeEvent; schema DirectCloseEvent; oracle _emit/receipt; golden and receipt assertions | Confirmed | Compatible ordered family with kind-derived reason and null System acting side. |
| Six forks, 30 artifacts, 12 cuts | Fixture lists six distinct owner/reason cases with five artifacts each; SixFrozenForksRetainThirtyArtifactsAndTwelveCuts; retained focused25 log | Confirmed | Both before/after cuts and exact retry reconstructed; count assertions prevent empty golden loops. |
| World/resources/RNG/route retained | ClosureState forwarding and Emit; SerializeState; LifecycleCodec.WriteWorld; property preservation and whole-World forgery tests | Confirmed | No resource, RNG, track or progress effect introduced. |
| Canonical/raw/re-signed forgery rejection | DeserializeInput roundtrip; Replay regenerated-event comparison; ReadState full-byte comparison; CanonicalAndResignedEffectsAndCachesReject | Confirmed | Re-signed mutations still cannot become accepted transitions. |
| Buffer isolation and strict bounds | Replay copies retained close bytes; immutable predecessor reconstruction; WholeWorldForgeryRejectsAndAllBuffersDetach; Parse/CheckArrays and null/depth/byte/item tests | Confirmed | No retained caller byte buffers become authoritative mutable state. Version/receipt caps present, unreachable in valid exact version13 profile. |
| Build/focused/full/boundary/format green | Actual retained logs and evidence commands below; five frozen hashes | Confirmed with stated limit | Logs inspected; no independent test rerun. Empty format log alone cannot establish exit status, so format exit0 relies on lead execution evidence. |
| Source pins and scope unchanged | Independent SHA-256 checks of all14 fixture sourceHashes and five primary hashes | Confirmed | Existing oracle/predecessor source contract remains frozen. |
| No general restore/runtime/scheduler proof | Internal types, unchanged public readers, supporting docs and explicit exclusions | Confirmed | Ready verdict restricted to F3 gate. |

## Verification Performed

Independently executed read-only checks:

- `git status --short`, `git branch --show-current`, `git rev-parse HEAD`, `git diff --stat c83d7c4`, `git diff --numstat c83d7c4`, supporting-path diffs: expected feature branch/base and five-primary-file scope confirmed.
- `shasum -a 256 -c .planning/combat-task008-delivery/f3-source.sha256`: all five OK.
- Inline Python using `hashlib.sha256(Path(path).read_bytes())` against fixture `sourceHashes`: 14/14 matched; fixture inventory six cases/30 artifacts.
- `git diff --check`: exit0, no output.
- Read exact three implementation files, entire380-line test file, test-project fixture diff, closure prose/schema/oracle initial/command/authorize/_emit/replay/retry/read-state/trace code, F1 trigger prose, relevant lifecycle helpers, and plan/navigation diffs.

Inspected retained execution evidence, not rerun:

- `/tmp/f3-reaction-baseline.log`: CMB-IRC PASS, six traces/six events/12 cuts/six retries/372 mutations/180 raw/126 boundaries/14 source pins.
- `dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --no-restore --filter-class '*CombatReactionClosureTests' '*CombatReactionTriggerTests' '*CombatReactionLifecycleTests' '/bl:/tmp/f3-focused-final-{}.binlog'`: `/tmp/f3-focused-final.log`, 25 passed, zero failed/skipped,35s069ms.
- `dotnet build Sandtable.slnx --no-restore '-bl:/tmp/f3-build-{}.binlog'`: `/tmp/f3-build.log`, build succeeded, zero warnings/errors,3.91s; actual binlog `/tmp/f3-build-20260920-021143--80401--Od1qp_.binlog` listed.
- `dotnet test --solution Sandtable.slnx --no-build '-bl:/tmp/f3-suite-{}.binlog'`: `/tmp/f3-suite.log`, 2,069 passed, zero failed/skipped,3m18s984ms.
- `dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --no-build --filter-trait 'Boundary=UserSpace' '-bl:/tmp/f3-boundary-{}.binlog'`: `/tmp/f3-boundary.log`,81 passed, zero failed/skipped,9s555ms.
- `dotnet format Sandtable.slnx --verify-no-changes --no-restore`: `/tmp/f3-format-full.log` empty, exit0 recorded in lead evidence; no independent exit-code provenance beyond retained evidence.

No build, tests, oracle rerun, code edit, Git mutation or delegation performed. Existing evidence sufficient for this bounded pass.

## Open Questions And Residual Risks

- Review independence: blind ledger predates author/evidence and session recall. Parent/bootstrap supplied scope plus claimed aggregate green checks. Code-only CCE searches yielded no author/prior-review content before ledger. Required post-ledger `session_recall("CampaignCombatReactionClosure")` returned one author code-area summary and unrelated historical review/decision summaries. Subsequent f3-evidence.md disclosed earlier F3 Ready verdict summaries; no earlier review report read. Those summaries were not used as correctness evidence.
- Exact-profile wrapper and state codec depend on lifecycle helpers and contain duplicated canonical layout. Future changes need golden regression checks across F1/F2/F3; no current inconsistency found.
- No independent execution of .NET or oracle checks; verdict combines direct code/plan/fixture/source inspection with retained frozen-source run evidence.
- Capacity beyond this version13, single-window, ordinary-infantry profile remains unsupported. No clock, host authority, public activation, durable recovery, active fallback, positive vehicle or second-move proof claimed.

## Verdict

Ready — frozen Task008 F3 direct closure implementation and bounded plan satisfy reviewed gate. No blocker or heavy pivot identified.

## Recommended Next Actions

Lead may record F3 acceptance and proceed with authorized delivery bookkeeping, retaining exact reviewed hashes and scoped test evidence. Keep F4–F6/H/runtime/publication gates open. Review instance3of3 complete; no additional review or experiment started by this reviewer.
