# Task013 dispatch proposal — research only

Research while011 root gates/review1 run.011 worker frozen,012 proposed/unimplemented. No013 implementation authorization. Preserve005/012→013→014–016 dependency order. Only this proposal written; no source/test edits, .NET, oracle execution, agents or commits. Exact012 API must be reread after acceptance.

## Scope decision

Implement authenticated ResultState2 initial→resolved and its one `combat-result-assault-resolved` event. Consume committed paid-use evidence, draw exactly eight role-ordered dice plus conditional capture share, atomically return result+final cursor+pending settlement World. Keep mandatory consequences unresolved, window null, closed=false and both closure receipts null. Reject future advance/choice/settlement/closure work; applicable stale callbacks remain no-ops and exact accepted resolve retries return original receipt. Do not allocate a new wire version or public projection.

Authoritative sources: `docs/design/combat-cycle-implementation-plan.md` Task013 row (currently897) and checkpoint F; `docs/specs/combat-result-settlement-v2.md` “Scope and authenticated predecessor”, “Result, World7 and closure”, “Canonical records”; exact ordered `combat-result-settlement-v2.schema.json`; `verify-combat-result-settlement-v2.py:73–105` context/committed predecessor, `:121–165` initial/resolve/resolved World, `:173–220` validation/retry/resolve, `:247–272` event/readback, `:304–321` full context authentication. Historical DES cost/result ordering supplies semantic rationale, not replacement v1 bytes or timing. Current011 and012 proposals govern independent-input provenance and debit boundary.

Positive source remains synthetic C3a with separately trusted Boundary, seed0 and explicit retained cursors; not actual campaign RNG play. Actual010C path has empty selection and never supplies these opportunities. No generic HistoryReplay, extended Snapshot12, host publication, public receipt/revision or all-runtime completion claim.

## Read-only fixture inventory

Exact fixture: `docs/specs/fixtures/combat-result-settlement-v2.json`, top-level `literalCases`8 and `traces`32 (8 branches×2 acting sides×2 seal orders). Full downstream corpus has272events and304state hashes; this is NOT013 scope.

013 subset: first result event of each trace (assert effect.kind=`assault-resolved`) gives32literal resolve events and64initial/resolved state-hash cuts. Every trace retains literal Base2, separate C3a predecessor, six RoundInputs and six literal RoundEvents, complete committedCanonicalUtf8, ResultInputs/events and stateHashes. Intermediate ResultState bytes are **not literal fixture strings**: only full final downstream stateCanonicalUtf8 is stored. Reconstruct typed initial/resolved bytes and compare SHA256 against stateHashes[0]/[1]; compare resolve event bytes literally. Do not label64cuts as64literal state JSON values. Remaining240events/240new downstream cuts stay014–016.

Sixteen traces resolve with8draws;16 with9draws. Twelve contain rejected random bytes. **Zero of32 literal resolve traces crosses a32-byte block boundary**; require separate supplemental boundary vector. Branch cursors: ordinary0, zero-retreat129, refusal-loss-dp160, zero-engaged35, defender-capture guard/escape16, attacker-capture guard/escape174118. All seed0. CP-limit branch starts Eattacker5/Edefender7; others0/0. Postcommit before result is10/10 CP for that branch, not later defender11 (which belongs to actual retreat014/015). Literal branch final TOE/DP/RP/guard/entitlement assertions are downstream facts and must not be applied at resolve.

Inventory was counted by loading JSON and inspecting event strings only, without importing/executing oracle. Full effect counts:32 each assault-resolved/disposition-recorded/losses-settled/retreat-settled/relationships-settled/round-closed/ca-completed/choice-opened,16custody-settled. Names identify captured role, not captor.

## Concrete reuse constraints and recommended narrow compatibility approach

Two real constraints prevent naive reuse:

1. `CampaignCombatSettlementState` in `CampaignCombatObligations.cs:230–246` requires commitmentId=`settlementId + ".commit"` and resultId=`settlementId + ".result"`. Frozen Result2 explicitly replaces these synthetic sample identities with independently derived `cmt.`/`res.`/`set.` hashes (`projected_world`, oracle156–164). Existing World7 constructor validates open-settlement elements against frozen paid preLossElements; preserve those checks. Passing Result2 IDs to current constructor fails before resolution can be represented.
2. Existing Identity grammar's `WriteSelectionStepsExternalSyntax` and `preserveArrayOrder` mode forbid Route and LegacyBrokenVehicleLot together (`CampaignCombatIdentityCodec.cs:227–241`). Result2 oracle `typed` at35–36 explicitly permits Route=id[] and futureTurn1..115 while retaining semantic arrays. Reusing C3a bridge unchanged for all Result2 World syntax would apply wrong contract. No existing result-v2 fixture project link; `Cna.Core.Tests.csproj:108` currently links Round2 only.

Recommended routine prerequisite within013: narrowly version-aware settlement identity admission plus a named Result2 grammar profile. Keep old synthetic constructor behavior and bytes untouched. Add an internal resolved-only factory that accepts generated commitment/result identities, derives exact `set.` hash from ordered `{commitmentId,resultId}`, and runs all existing opposing-participant/preloss paid-state/scope checks with all consequence receipts null. Private common construction can distinguish legacy identity rule from this explicit Result2 rule; never add an unvalidated public “skip identity validation” boolean. Returned typed World7 contains actual pending settlement; validate no other World facts differ. Future014–016 must extend reviewed identity admission for later receipts; do not implement their state changes now.

For grammar, expose a narrowly whitelisted Result2 external writer on Identity codec that preserves array order, allows Route according to Result2, keeps LegacyBrokenVehicleLot forbidden, and uses existing futureTurn bounds. Separate those flags/profile meanings instead of weakening C3a's forbidden Route rule. Existing inherited/default profiles remain byte-identical. New Result codec owns its local frozen descriptors and delegates transitive World/Result/UnitKey/Random types through this named profile; audit complete closure and special primitives, not just text field parity.

Alternative: distinct typed pending-overlay held beside unchanged committed World, with serializer composing World7 settlements. This avoids changing shared constructor but introduces a second representation of pending World authority and later014 adapter work. It is viable only as an explicitly reviewed internal typed projection (no arbitrary JSON), not an excuse to bypass World7 invariants. Prefer narrow shared-model identity factory because frozen Result2 really contains World7 settlement and existing validators already cover paid preloss state. Do not choose overlay silently during implementation.

## Material file plan

Five **material** paths feasible by keeping bounded state/effect models next to their one-transition engine (existing010A module precedent), plus one mechanical fixture-link edit:

1. New `src/Cna.Core/Campaigns/CampaignCombatResolution.cs`: immutable result/draw/input/state/effect/result-return models and private authenticated initial/resolve/fold/replay. One transition, not whole settlement state machine. No line-limit compression.
2. New `src/Cna.Core/Campaigns/CampaignCombatResolutionCodec.cs`: frozen local Result2 grammar, typed writers, strict event/state readback, owned canonical World mapping from actual typed committed/resolved World and result. No broad generic serializer.
3. Existing `src/Cna.Core/Campaigns/CampaignCombatObligations.cs`: narrow resolved-only Result2 identity factory/common constructor adaptation, preserving legacy synthetic identity validation and every existing structural/conservation check.
4. Existing `src/Cna.Core/Campaigns/CampaignCombatIdentityCodec.cs`: explicit Result2 transitive syntax bridge/profile preserving old profiles; no repeated World descriptor inventory.
5. New `tests/Cna.Core.Tests/Campaigns/CombatResolutionTests.cs`:32literal resolve events64hashcuts, supplemental RNG boundary and context/atomicity/strictness proof; targeted legacy model/grammar regression here or existing tests invoked unchanged.

Mechanical sixth physical path: `tests/Cna.Core.Tests/Cna.Core.Tests.csproj`, add single existing result-v2 fixture CopyToOutputDirectory link in established location. Root must explicitly retain this scope in dispatch; this is not a sixth material architecture component and does not alone require user approval. Do not read fixtures from guessed repository-relative runtime paths or copy/embed new fixture bytes to evade link. If root insists on exactly five physical paths, scope cannot honestly satisfy fixture linkage and both compatibility needs without a separate bounded prerequisite/refinement. No implementation until root chooses/finalizes scope. No need modify SandtableRandom, adjudication tables, Round engine or World7 constructor.

## Authentication and APIs

Use same independent authority tuple as012: request + retained Created + trusted CombatStepsBoundary + predecessorInputs/events + accepted RoundInputs/events. Rebuild accepted012 through commit (status committed, step5/five step receipts, one history/target use, paid World, retained RNG). Derive exact committed bytes/hash internally. Optional untrusted committed bytes supplied to a reader must compare against this reconstruction; never trust a standalone Committed state. Do not expose prior ResultState as Apply authority.

Proposed `ReplayTrustedBoundary(...predecessor tuple..., roundInputs, roundEvents, resultInputs, resultEvents)` and `ApplyTrustedBoundary(..., trustedResultInput)` keep independent accepted ResultInputs apart from event bytes. Raw `ReadState(bytes,...full tuple...)` parses full closed grammar/canonical first, then replays context/local stream and compares whole bytes. Context struct, if used to reduce signatures, must own bounded inputs and cannot stand in for replay. Capture Created before any caller list access; bound each sequence and byte value before allocating copies. Base/Round limit16; Result sequence32, value1MiB/depth32/arrays512, signed64 authority and UInt64 RNG; no narrowing cursor to long.

ResultState initial: status committed, current head from committed, committedHash SHA exact RoundState2, roundClockConfigurationHash from Round2, resultClockPolicyId=`sandtable.combat.mandatory-window-clock.v2`, original paid World/RNG, result/settlement/window/closure IDs null, receipts empty,closed=false. acceptedHighWater equals committed public opening floor, not max private seal time. Resolve has null time/available confidence so audit value remains unchanged; mandatory-window timing is014+, no clock service added.

## Draw and result algorithm

Reuse `SandtableRandom.NextByte` (`Randomness/SandtableRandom.cs:21`) to retain every consumed byte; its checked cursor advance guards UInt64 overflow and deterministic32-byte SHA256 blocks. RollD6 alone omits rejected-byte evidence, so a small local draw loop repeatedly calls NextByte until byte<252 and derives byte%6+1, recording purpose,beforeCursor,afterCursor,die,consumed. Bound produced arrays before publish. Never seed a new stream, prefetch ninth byte or mutate committed cursor.

Take exact eight ordered purposes from pinned `Cna1979CombatAdjudication.Definition.Procedure`; attacker Morale pair, defender Morale pair, attacker assault pair, defender assault pair. Derive two coordinates and both Morale adjustments with existing `LookupMoraleAdjustment`; differential from same two preloss sides. To select conditional ninth purpose, inspect pinned effect row's capture sums using each side's own ordered assault pair sum. Only triggered role gets capture-share draw. Call existing `Cna1979CombatAdjudication.Resolve` (`:37`) with all four coordinates and optional captureDie, refusal=false. Reuse returned selected facts; do not duplicate tables or prematurely apply rounded casualties/refusal increments. Both paid sides still have frozen10TOE/rating1/cohesion0; never apply attacker loss before calculating defender result.

AssaultResult binds commitment, RulesInputHash, procedureId, before/after Random, complete draws, both Morale coordinates/values, basicDifferential0 and exact Result facts (including rawEngaged/requiredRetreat/capture). `res.` digest excludes resultId; `set.` digest binds commitmentId/resultId. Form typed pending settlement with both paid preLossElements and all disposition/loss/retreat/custody/relationship receipts null. Result World differs from committed World only by this singleton settlement; no ammo/cost/TOE/location/cohesion/relation mutation. Set status resolved, result/settlement IDs and final RNG, append one full event/receipt/head atomically. Keep round/history/target use in authenticated predecessor, not copied from caller cache.

Event explicit clock bindings lead version2 as schema dictates. configurationHash is original Config1 (unlike RoundEvent supplemental configurationHash); roundClockConfigurationHash remains separate. Receipt `cmb.` D(result-receipt.v2,unsigned full event), then prefix fold complete bytes. No circular digest. Validate primitive/context/kind/actor/nullability before Command+actor retry; exact resolve retry before stale version/time/status, never redraw. Fresh resolve requires committed status, exact version, null decision/choice, System, null time/available=true. Stale/no-window System expire/unavailable no-op. New advance/choose/settlement/closure rejects pending014–016. Overflow/failure rejects without cancellation/refund/alternative seed.

## Failure-sensitive tests and cumulative evidence

First RED should prove authentic paid012 trace cannot yet emit literal resolve event. Once implementation exists, test all32full independent contexts; every first event literal, both initial/resolved whole serialized hashes against stateHashes0/1; strict readback/restart, with complete predecessor input/event chains and no inferred event.input trust. Cross-side/seal order facts equal by role where appropriate, causal IDs need not equal. Assert no new costs/history or settled losses/retreat/custody, no window/closure and no Reserve Release.

Reuse005 independent full selected tables proof (`CombatSelectedRulesTests:102–168`:1296Morale,6480joint,8840settlement cases) and009B complete support certification rather than duplicating Resolve or enormous enumeration. New integration proves actual draw coordinates feed same verified adjudicator and exact pinned procedure. Literal32 does not by itself certify every possible outcome.

Supplemental block crossing: existing frozen research vector `docs/research/verify-combat-rng.py` VECTORS seed0,cursor30 gives consumed hex `4dd79fabb6070dc7`, dice66443222, finalcursor38, differential-1,base loss10/10,no capture. Rebuild synthetic Boundary/C3a/Round context at cursor30 through runtime APIs; do not inject arbitrary Committed cache. Assert raw bytes/purpose/per-die cursor and result facts, labeling supplemental existing research evidence, not new literal Result2 event golden. Seed0,cursor129 literal fixture covers rejectedbyteFE and later acceptance; require consumed bytes retained. Existing RandomStreamTests provides independent stream/rejection/overflow vectors. Test UInt64.MaxValue and near-end authenticated cursor overflow: no state/head/result change; no fallback.

Core atomic candidate discard: resolve locally then discard result without appending event; retained committed bytes/World/cursor unchanged. Retrying original tuple reproduces identical result/event; after acceptance, exact command+actor retry returns old receipt/event without append or draw. Failed candidate serialization/overflow/invalid evidence similarly leaves prior state. This is dormant Core discard/recovery proof, not host durable transaction injection; host publication remains separate.

Negatives: missing/extra/changed predecessor/round inputs/events; foreign committedHash/role/seed/cursor/world; altered paid ammo/Cohesion; wrong policy/parent config/round/commit/version/actor; malformed primitive before retries; noncanonical/missing/duplicate/unknown fields before sentry context; rehashed draws (purpose/order/before/after/consumed/die), swapped side facts, unused ninth die, missing capture die, altered result/settlement IDs, forged pending World/preloss elements, changed final cursor; duplicate/reordered/excess local events; every later fixture event rejects within013 scope. Test legacy constructor still rejects independently hashed IDs on legacy path, new factory derives exactsetID and rejects wrong-format bindings, and both retain paid-world invariants. Result2 grammar permits syntactically valid Route arrays/futureTurn while C3a forbids Route; whole replay still rejects unearned later settlement state. Returned arrays/inputs/World/receipt buffers owned. Run selected Rules/Randomness/World/Identity/CombatSeals/CombatCommit regressions as warranted; root full suite/reviews afterward.

## Decision needed before dispatch

No frozen contract contradiction: Result2 explicitly replaces sample settlement IDs and widens Route syntax, so compatibility changes are required implementation work. Recommended version-aware factory/grammar adapter is bounded and preserves old behavior; root can authorize five material paths plus mechanical fixture link under existing delivery scope. Distinct pending overlay is an alternative architecture choice, not default. Root should finalize this after012 acceptance and exact API inspection; no new spike or user permission is inferred solely from six physical files. Actual positive history, host atomic publication, public timing/observations, Snapshot12 and all mandatory settlement remain open.013 closes only result/cursor+pending obligations, never settled assault.
