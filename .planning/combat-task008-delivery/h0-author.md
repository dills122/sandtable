# Author Explanation — H0 inherited literal Snapshot12

## Intent and success criteria
Add a contract packet for actual selected B–G/019A and F1–F6 retained cuts. Existing creation-only
Snapshot12 and synthetic first-Combat composition cannot represent these cuts. Preserve exact19root
fields and creation bytes while defining closed typed arms and equal bytes for identical histories.
Base45a188258d6bac3d81515d49466b615c0b9e9a20; four new primary paths in h0-source.sha256.

## Plan-to-implementation traceability
Initial H requires complete trusted replay with fresh admission disabled. H0 provides only the
contract needed before runtime work. H1 pre-cycle, H2 Movement/Breakdown, H3 Reaction routing and
H4 root codec/admission-disabled seam remain pending; H5 cumulative evidence if needed. No later
Task009–019 implementation, all28 runtime trace closure or HOST-PUB-001 persistence claim.

## Technical approach and components
Spec defines trust/normalization/compatibility; schema declares exact root order, two cycleState
arms, position/flow unions and qualified predecessor types. Oracle composes only from actual family
replay, never expectedState. Fixture retains complete literal bytes, hashes, lengths, history identity,
shared-cut groups and source pins. Complete receipt ledger and prefix independently checked.
B2 Reserve-entry normalization invokes actual Reserve initial replay so overlapping interfaces agree.
F6 supplied complete F5 transcript is replayed and compared to its case-based predecessor interface.
Readback validates canonical structure then compares complete root against independently retained
trusted request/Created/history. This is an evidence adapter, not a generic runtime router.

## Decisions and invariants
Additive packet leaves pinned predecessors untouched. New reserve-designation/inherited-cycle arms
retain members, cycle/route/progress/interruption/end proofs; no opaque family-state blob or duplicate
World. Pre-route idle and absent slots require actual causal absence; live flow is copied. Initial H
combatState stays null, including G2. Later reachable Combat composition must retain this evidence.
Publicly recomputable hashes are integrity bindings, not evidence of which legal branch committed.
Memoization uses bounded immutable full inputs; mutation changes keys and forces actual replay.

## Verification performed and results
See h0-checks.md and worker handoff for exact normal verification, predecessor checks and failures.
Selected inventory368cuts:312explicit fixture cuts plus56supplemental prefixes,286uniquehistories
and62sharedgroups. Final execution evidence is recorded in h0-checks.md. Measured maximum root20242B,
history21events/57445B, structuraldepth9/array21. These are selected-profile measurements, not
campaign-long capacity guarantees. Frozen manifest defines review target; reports determine readiness.
No C# changes: prior accepted F6 full2104/boundary81/build/format remains prior checkpoint evidence.
Local526documentation link targets checked; Lychee unavailable, no full anchor-check claim.

## Costs, deviations and challenge points
Full literal fixture is sizable but inspectable generated evidence. Oracle imports a broad pinned
predecessor graph and uses family-specific adapters; runtime routing must remove caller family
selection. Roughly two-minute verifier cost reflects actual repeated history replay. Bounds target
selected profile. Review causal normalization, full-ledger preservation, representative nested and
recomputed-binding falsification, actual source-pin graph, cache-key trust, shared cuts, and distinction
between contract proof and future runtime/admission/publication gates. No architecture pivot.
