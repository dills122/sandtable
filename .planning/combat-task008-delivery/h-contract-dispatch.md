# Initial H contract audit dispatch

Research unit, not an independent implementation-review round. Start after F6 accepted.
Decision owner: lead. Question: smallest contract-first packet required to prove complete literal
Snapshot12 restore for every implemented B–G/019A/F1–F6 cut with fresh admission disabled.
Read AGENTS, research-to-decision, Task008 H canonical plan, C2 creation envelope, snapshot-composition
schema/spec/oracle, authority-composition schema/oracle, actual accepted family schemas and C# codecs.
Read h-restore-research.md as provisional testimony, not authority.

Own only h-contract-audit.md. Shared repo, preserve others. No code/canonical edits, git, external
publication, storage-provider selection, public activation, subdelegation or expensive .NET builds.
Cheap read-only experiments allowed with exact commands/results. CCE first, precise source fallback
if compressed chunks cannot expand. Separate documented fact, observation, inference and unknown.

Return existing literal-root coverage versus family caches/RootSnapshot1 witness; exact missing
arms and retained fields; smallest additive H0 contract packet and subsequent bounded runtime
router/codec/test slices. Preserve creation12 and historical first-Combat arm bytes, all source pins,
sole current World, exact root field order and trusted-history provenance. Address pre-cycle state,
Reserve membership, cycle/opening identity, tracks/progress, Reaction windows/flows, G2 movementEnd/
interruptContext/breakdownCompletion evidence without opaque caller-state blobs or synthetic defaults.
Never select event semantics by eventType alone: actual replay state distinguishes competing paths.

Compare options and document consequences for later009–019 extension. Initial H must not invert009
dependency by requiring future handlers, or falsely close cumulative28 traces or HOST-PUB-001.
Explicit policy proof: trusted retained creation/history accepted with admission disabled; absent
retained creation rejected. Recomputed hashes alone cannot authenticate which legal fork committed.
Stop after evidence-backed recommendation and remaining precise design questions; lead finalizes
scope and owns implementation and required dev+three independent review gate for each new slice.
