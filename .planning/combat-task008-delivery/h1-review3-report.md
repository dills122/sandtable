# H1 independent review

Review instance: 3 of 3.
Target: d684be88d34c7b1e444aad6e173213efa647c2f5; base 4139675fc9b775c5e7c0d355c06756c17ea1f2f9.

## Blind preliminary ledger

Recorded before author/checks/session recall. Git HEAD matches bootstrap and working tree initially clean. Read targeted five-path diff, canonical Initial H/H refinement and literal inherited Snapshot12 contract, plus actual preamble, Weather, stage and Reserve opening readers. No earlier review report or aggregate evidence read.

- Ownership: Capture checks count, retained Created11 size, each event size and aggregate size before payload copies; snapshots input list references once and copies payloads. Returned buffers copy again. No caller buffer authority alias found.
- Routing: fixed counts partition selected profile, while strict predecessor readers validate each causal gate before successor routing. Stage terminal state10 reaches Reserve reader with empty Reserve suffix, materializing membership. Reserve reader rejects unsupported completion tails. No count-only admission found.
- Evidence: selected case identity equality detects missing or extra frozen precycle histories; each prefix compares head, receipt ledger, position, shared state and Reserve/cycle evidence. Separate predecessor goldens compare canonical typed bytes. Negative tests cover missing creation, omission, duplication, swap, malformed mutations, foreign histories/request, forged canonical Weather, valid unsupported Movement, bounds and buffer ownership.
- Plan: H1 intentionally stops at first opening. Full Snapshot12 codec, actual disabled-admission restore, Movement/Breakdown and Reaction routing remain H2–H4. No full-restore claim justified or made by inspected code.
- Preliminary result: no actionable defect established. Remaining checks: frozen digest verification, author-claim reconciliation, focused executable test evidence, supporting documentation scope.

## Findings

No actionable findings. Five primary paths remain unchanged against frozen manifest. Changes are additive internal Core replay/evidence types, focused tests, and one fixture link; predecessor code, schema and fixtures are unchanged.

## Plan Review

Ready for bounded H1 objective. Canonical plan lines819 and831–841 separate H1 precycle routing from H2 Movement/Breakdown, H3 Reaction, H4 literal-root/disabled-admission restore and HOST-PUB-001 durable publication. Current implementation follows this split without dependency inversion or invented public admission.

`CampaignCombatHistoryReplay.Replay` validates actual opening before Weather, Weather before stage, and stage before Reserve. Its fixed partitions are justified by selected profile's four/one/four causal gates, with canonical admission delegated to unchanged strict readers. At nine post-creation events, Reserve is materialized even without a designation/completion event; this matches H0 shared-cut requirement. All later tails reach strict Reserve opening rejection rather than silent truncation.

README, tech-design, naming-overview and execution ledger consistently describe in-progress H1 and retain full-restore/publication limits. No migration, rollback or operational activation change is needed for these dormant internal types. Their repeated predecessor replay and bounded copies incur accepted small-profile cost, not a new unbounded runtime path. Closure of H1 should update status only after lead's required CI gate; H2–H4 and publication stay open.

## Author-Claim Reconciliation

| Author claim | Evidence inspected | Status | Review consequence |
| --- | --- | --- | --- |
| Owned Created11 plus bounded event stream; no missing-creation fallback | `CampaignCombatRetainedHistory.Capture`, copy-returning accessors; missing/bounds/ownership tests | Confirmed | Input and exported buffers cannot mutate retained authority after capture. Capture alone is not authentication. |
| Counts locate partitions while strict readers prove causality | Router plus opening/Weather/stage/Reserve readers; every-occurrence negatives | Confirmed | No acceptance shortcut from event count or family label. |
| State10 materializes Reserve membership; atomic completion preserves inherited cycle | Router's nine-event boundary; prefix comparisons and predecessor exact goldens for both owners/designation choices | Confirmed | Shared stage/Reserve cut preserves actual authority. |
| All selected precycle H0 histories covered | `SelectedCasesCoverEveryFrozenPrecycleHistoryIdentity`, per-prefix theory; separate fixture inventory | Confirmed | 256 vectors, 208 unique histories; no missing selected prefix identified. |
| Truly re-signed forged Weather rejected | `CanonicallyResignedForgedWeatherStillRejectsCausalRngMismatch` uses typed event serializer then actual router | Confirmed | Negative probes causal RNG binding, not merely invalid JSON/hash formatting. |
| Lawful future Movement rejected | `ValidFutureMovementTailRejectsWithoutTruncation` constructs actual accepted Move4 before router rejection | Confirmed | Unsupported suffix fails closed. |
| Local focused/full/build/boundary gates passed | Independent focused run; retained suite/boundary/build log tails | Confirmed within stated evidence | Reviewer reran focused tests only; full-suite results are retained execution evidence, not independently rerun. |
| Full solution format passed | h1-checks.md reports exit0; scoped source diff clean | Reported, not independently rerun | No formatting defect found; do not mislabel as reviewer-executed check. |
| No full-root, disabled-admission or publication closure | API surface and canonical plan; supporting docs | Confirmed | These gates remain outside H1 verdict. |

## Verification Performed

- `git rev-parse HEAD`: d684be88d34c7b1e444aad6e173213efa647c2f5. Initial `git status --short`: clean.
- Targeted five-primary-path base/head diff reviewed before author evidence; supporting docs and changed-path inventory checked afterward. Remaining changed planning files are delivery evidence; prior review reports and aggregate h1-evidence were not read.
- `shasum -a 256 -c .planning/combat-task008-delivery/h1-source.sha256`: all five OK.
- `git diff --check 4139675fc9b775c5e7c0d355c06756c17ea1f2f9 d684be88d34c7b1e444aad6e173213efa647c2f5`: exit0, no output.
- Native MTP/xUnit SDK-style configuration checked in global.json, test csproj, Directory.Build.props and Directory.Packages.props.
- Independent command: `dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --no-build --filter-class '*CombatHistoryReplayTests' '-bl:/tmp/h1-review3-{}.binlog'`, login:false, escalated for documented local IPC. Exit0; 53 passed, 0 failed, 0 skipped; 6s792ms. Log `/tmp/h1-review3-focused.log`; actual binlog `/tmp/h1-review3-20260920-045235--97772--CRVqLU-dotnet-test.binlog` confirmed present. Process6299 completed before verdict.
- Independent read-only Python fixture inventory selected null/reserve-designation/newly opened inherited-cycle roots:256 vectors/208 exact creation-plus-event-hash histories.
- Retained `/tmp/h1-suite.log`:2157passed/0failed/0skipped; `/tmp/h1-boundary.log`:81passed/0failed/0skipped; `/tmp/h1-build.log`:0warnings/0errors. No unnecessary full-suite/oracle repeat.
- Required session recall performed only after blind ledger; recalled decisions treated as context, not proof.

## Open Questions And Residual Risks

No unresolved H1 blocker. Fixed profile routing must be extended deliberately for H2/H3 rather than treating later event counts as admission. H4 still must validate literal root/head agreement and actual disabled-admission behavior; retaining valid shorter histories here is intentional because H1 receives no committed-head claim. Durable archive authentication and process restart remain HOST-PUB-001.

Independent run reused root-built artifacts with `--no-build`; source hashes and root successful build evidence support artifact alignment. Remote exact-head CI is concurrently owned by lead and was not queried or claimed passing by this reviewer. Lead must await those checks before H2 dispatch. Report readiness concerns reviewed H1 code/plan, not remote gate completion.

## Verdict

**Ready** — bounded H1 implementation and plan. No actionable findings or heavy pivot. Review instance3of3 ends here; no additional review/workstream started.

## Recommended Next Actions

Lead awaits exact-head remote CI, reconciles this report, then records H1 acceptance and proceeds under existing H2 scope. Keep H4/full restore and HOST-PUB-001 explicitly open.
