# Task012 dispatch proposal — research only

Research after010C worker freeze while root full gates/reviews run.010B accepted762468e/fe5664d;010C not yet accepted;011 proposed but unimplemented. No012 implementation authorized. Canonical dependency remains010→011→012→013. Only this proposal edited; no source/tests/.NET/oracle execution/fixture regeneration/agents/commits. Freeze exact names against accepted011 before dispatch.

## Decision and bounded output

Extend authenticated sealed-round-v2 engine through its existing `commit-attack` transition. One atomic Core result changes both CP ledgers, both ammunition balances, commitment occurrence, directional attack history and segment target use; adds one event/receipt/version and retains RNG. Keep stepIndex5, closed=false, status=committed. Do not complete Close Assault, resolve dice, settle consequences, advance Reserve/cycle, or introduce a public/persistence contract.

Canonical authority: `docs/design/combat-cycle-implementation-plan.md:891` Task012 and next row013; `docs/design/combat-cost-resolution-order-v1.md` CMB-RES-001/002/007 and “Admission and irreversible costs” (eligibility-at-use, cumulative ledger, failed publication); `docs/design/combat-opportunity-identity-v1.md:150–169` (directional once-only edge, distinct target-use scope); `docs/design/combat-sealed-decision-protocol-v1.md:200–209` (allowed structural suffix, not whole-position equality). Active byte/type/clock authority is `docs/specs/combat-sealed-round-v2.md`, `combat-sealed-round-v2.schema.json`, and `verify-combat-sealed-round-v2.py:214–358`. Historical design high-water wording is superseded by v2 immutable public opening floor; do not revive it.

Actual creation-rooted history currently supports empty010A/010C only. Positive commit derives from independently trusted synthetic C3a Boundary plus authenticated010B/011 histories. Neither typed validation nor new commitment authenticates a positive campaign history that does not exist. Generic HistoryReplay and Snapshot12 remain outside this slice.

## Five material primary paths

1. `src/Cna.Core/Campaigns/CampaignCombatSealedRound.cs` (planned011): implement commit guard/effect/fold inside existing private transition, preserving full replay API and retry order.
2. `src/Cna.Core/Campaigns/CampaignCombatSealedRoundModels.cs` (planned011): typed immutable cost, directional history and target-use entries, committed effect/state fields if011 did not already materialize full frozen inventory. Reuse existing World7/element/ammo/operational records; no new wire certificate or independent payment object.
3. `src/Cna.Core/Campaigns/CampaignCombatSealedRoundCodec.cs` (planned011): exact frozen commit writers and committed World serialization; full causal state/event readback. Reuse existing011 closed grammar and010B external syntax bridge unchanged.
4. New `tests/Cna.Core.Tests/Campaigns/CombatCommitTests.cs`: exact literal commits, restart/discard/retry, costs/limits, binding/forgery/ownership checks.
5. `tests/Cna.Core.Tests/Campaigns/CombatSealsTests.cs` (planned011): update narrowly scoped “commit pending012” rejection expectations to accepted extension, keep unsupported early/cancelled/closure negatives, and expose/reuse a small existing authenticated fixture builder if needed. Do not duplicate entire predecessor generator or rewrite011 tests.

Existing Round2 fixture already linked by009B; no csproj/schema/oracle/fixture change. Worker evidence separate. Root owns documentation/fullgates/reviews. If accepted011 needs fewer model/codec edits, do not manufacture changes to consume five slots. If a sixth path is materially required, report before implementation. No new commitment service, generic world codec, publication adapter or architectural split needed.

## API and trusted proof

Keep011 proposed `ReplayTrustedBoundary` / `ApplyTrustedBoundary` parameters: creation request, retained Created, independently trusted CombatStepsBoundary, separate010B predecessorInputs/events, separate accepted RoundInputs/events; fresh input and admissionEnabled for Apply. Do not add a public transition accepting arbitrary Prepared/Committed state. `ReadBase`/`ReadState` retain parse/closed-shape/canonical-before-context, then full authenticated reconstruction and whole-byte comparison.

Every application authenticates Created, validates trusted Boundary/009B support and replays010B to selected non-null accepted decline at step3, then reconstructs011 round including both seals and exact FA/empty-AA receipts. Direct internal transition only receives this derived prior. Persisted event.input is not independently trusted. Frozen base-equivalence permits certified structural step progression; World/resources/participants/RNG must still match opening Boundary before first commit. Re-running initial-ammo10 admission against post-debit World is incorrect: revalidate original authenticated Boundary, then replay debit exactly once.

After commit, retained Base2, allocations, costs, single history/target entry, event receipt and preResultRandomState constitute eligibility-at-use evidence. Existing state/receipt APIs can carry this proof into future013. Do not add an unused “paid certificate” API. Readback of exact committed state with ammo0 succeeds; another fresh use is unsupported/depleted and cannot create a new receipt. Resolution/surrender behavior itself remains013+, so012 must not claim an implemented result path merely from retained paid evidence.

## Exact transition and immutable fold

Follow oracle lines214–246: validate input primitives, context, kind-specific fields and actor before hash retry. Command only is hashed; actor recovered separately. `commit-attack` requires System, supplemental clock hash, current segment/round, expectedPriorVersion, null slot/allocation. Exact accepted command+actor retry precedes committed-status/time/resource gates, recovers original receipt/event without recharging; malformed primitive still rejects. Legal changed time metadata may recover retry as specified; fresh commit requires admittedAt=null and clockAvailable=true. Admission disabled blocks fresh opening only; existing Prepared commit and retained committed readback work.

Fresh commit (oracle305–320): status Prepared; stepIndex5; exactly five stepReceipts; both role-ordered slots sealed with exact full10 allocation; correct round and version; world/RNG equal frozen Boundary; no attackHistory/targetUses. Missing/reordered/changed structural proof is rejected by prior replay. No clocks/provider inputs can cancel Prepared or committed state. No mutable cache may supply missing proof.

Derive `cmt.` + D(`sandtable.combat.commitment.v2`, `{roundId,priorVersion,priorPrefix,allocations}`), allocations attacker then defender regardless seal arrival order or faction. Original configuration stays predecessorConfigurationHash; supplemental configurationHash remains v2. Event receipt uses full unsigned RoundEvent2 with existing domain; prefix folds complete event once.

Cost guard: integer existing E, carried ammo exactly10, attacker E+5<=10 and defender E+3<=10. Use pinned selected rules costs and existing exact arithmetic; confirm selected constants match frozen5/3/10. Preserve ledger turn/stage and origin. `CampaignCombatSpending.ChargeOrdinary` already returns immutable operational state and unchanged causes when no excess, but its general ordinary ceiling is15 for CPA10: explicit selected E+cost<=10 guard must precede it. It must produce no Cohesion cause/change here. Reuse helper after event receipt derivation if passing receipt identity; no circular receipt hash because cost balances/effect derive independently before fold.

Build both new element records and new World7 locally. Preserve location, Reserve status, components/TOE, parent formations, movement-ended/breakdown state, readiness and ammunition origin. Set each ammo10→0 and cumulative CP+5/+3. Existing `CampaignElementAmmunitionState` (`CampaignCombatWorldV7.cs:70`) supports zero; element constructor at108 and World7 constructor already provide immutable copies. Preserve all World collections/relations/causes/representation bindings. No World source edit required.

Append exactly one `AttackHistory` entry `{commitmentId,cycleId,segmentId,attacker,defender,targetLocationId,gameTurn,operationStage}` from authenticated Candidate, and one `TargetUse` `{commitmentId,segmentId,targetLocationId}`. Segment already binds cycle/acting-side occurrence; do not introduce a global target lock or reverse edge. Status committed, commitmentId assigned, one local receipt/version/prefix; existing opening, seals, five steps and timing unchanged. World and event published as one returned immutable Core result only after complete validation/serialization succeeds. No intermediate charge command/state, no RNG access and no refund path.

Codec World mapping:011 proposal retains owned canonical World from authenticated typed Boundary for unchanged precommit states.012 must extend that internal mapping to reflect typed derived CP numerator/denominator and ammo points, preserving all other canonical fields/order. Never invoke initial-world validation on depleted World, and never accept caller JSON as authority. Such bounded mapping parallels existing010B `WriteValidatedBoundary` at71–108; keep it local to Round codec. Round grammar already names Cost/AttackHistory/TargetUse; syntax recognition is distinct from actual causality.

## Exact fixture inventory and cumulative evidence

Read-only JSON inspection of `docs/specs/fixtures/combat-sealed-round-v2.json`:

-10 cases total;58 literal event strings,68 stored state strings including initial cuts.
-4 committed traces: both acting sides×both first-seal roles; each6events/7states. Their final eventType=`combat-attack-committed`, effect.kind=`attack-committed`; priorVersion32→stateVersion33; stepIndex5,closed=false; one attackHistory and one targetUse.
-All four retained cost arrays, in role order, are CP0→5 /0→3 and ammo10→0 for both. Seal ordering changes causal IDs/bytes, not role costs/gameplay facts.
-011 subset54events/64states excludes these4final events/states.012 adds4new events/4new cuts, completing58/68. Four paid traces alone24events/28cuts; six cancellation traces34events/40cuts remain unchanged regression evidence.
-All10 Base occurrences retain separately supplied historical010B predecessor input/event tuples; do not derive expected events or state from runtime to validate literals. Clock24 outcome rows remain011 regression authority, not newly counted012 cases.

At every full retained cut: exact literal state readback then suffix replay to final literal; include initial, before commit and after commit. Compare complete event/state bytes plus IDs, ledger balances, history direction/scope, target use, pre-result RNG and head. Existing01154/64 checks remain cumulative; no new fixtures or claims of58 distinct histories. Historical v1 bytes remain unchanged/rejected by active v2 reader as011 already requires.

## Required additional tests beyond four goldens

Meaningful first RED: accepted011 rejects final literal commit from fully authenticated Prepared step5; then implement atomic extension. Focused CombatCommit plus existingCombatSeals/shared010B tests. Keep exact failures, commands/binlogs, hashes and closed-process handoff.

1. Exact CP boundary supplements: independently trusted synthetic profile with Eattacker5/Edefender7 commits to10/10; retained fixtures prove0→5/3. One over, fractional CP, changed Cohesion/readiness, ammo9, extra participant or foreign origins rejects before active choice through existing009B validator. Reuse its comprehensive certification proof instead of duplicating support enumeration. Supplemental accepted histories must be rebuilt through existing010B+011 APIs from changed trusted Boundary; cannot change only final state or call internal transition with invented Prepared.
2. For first commit, wrong actor/round/segment/config/version, missing seal, FA-only step4, cancelled/closed state, reordered/forged empty-AA receipt, interleaved resource/world change or foreign same-looking Candidate rejects. Rehashed costs/allocations/before-after ammo/CP/commitment/history direction/target/RNG/event prefix and canonical committed root forgeries reject actual replay. Whole syntactic bounds/types/order reject before null/sentry context, including nested cost/unit/history and arrays; preserve1MiB/depth32/512/16 limits and signed64/UInt64 distinctions.
3. At successful commit, both costs/history/target use appear together; unchanged RNG/TOE/position/Cohesion/movement/clock. Compare role effects across four traces while allowing authority bytes/IDs to differ. No separate “apply costs” API.
4. Simulated failed publication at dormant Core boundary: compute candidate result then deliberately discard it without appending; previous immutable state, accepted lists and replay bytes remain identical. Retry from original retained prefix reproduces identical event/result; accepting it then losing response and retrying against appended history returns same receipt with no new append, charge or history duplicate. Invalid derivation never mutates prior inputs. This proves Core candidate atomicity/discard/recovery, not host database atomicity; no injected publisher abstraction solely to claim host proof.
5. Exact committed retries with unavailable/null fresh metadata and admissionEnabled=false recover original command/actor receipt before status/resource gates; changed command or owner cannot double charge. Fresh commit uses structural null/available input. Timers/unavailable callbacks after Prepared/committed remain no-op; changed player choices cannot reopen. Repeated event append rejects strict history. Read committed state ammo0 successfully; original eligibility-at-use evidence remains available for future013. New fresh assault/positive CA completion/result/settlement continuation remains rejected.
6. Ownership: buffer/list mutation cannot alter frozen Created, Base, accepted inputs/events, result costs/history/World or prior state. Keep get-only dependent cached state/ledger invariant. Test no partial charge when any guard fails and serialization bounds fail; do not fabricate reachable max-length histories if finite mechanism cannot produce them.

## Feasibility, blockers and closure limits

No contract contradiction found. Existing frozen Round2 already contains exact commit effect/cost/history/state shape and pure transition, so012 is a bounded extension, not a new spike. Five paths feasible contingent on011 following its accepted immutable replay/codec design; actual011 APIs must be reread before dispatch. Parent011 acceptance is real dependency, and011 itself still waits on010C acceptance.

Full production failed-publication/host recovery, positive creation-rooted opportunity admission, Snapshot12 persistence, result resolution and ammunitionless-surrender resolution are not implemented by these contracts. Preserve canonical012 paid-use requirement as retained eligibility-at-use and successful depleted-state readback; future013 must consume that proof without re-running pre-use ammo checks against depleted state. Do not quietly expand into013 to demonstrate a result. If root requires host transactional fault injection rather than bounded dormant Core discard/replay proof for012 acceptance, that is a genuine additional architecture/scope dependency requiring explicit dispatch revision, not a reason to invent an adapter here.
