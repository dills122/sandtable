# Task014 independent review

Review instance: 1 of 3. Reviewer mode. CCE disabled; no agents, source edits or Git mutations.

## Findings

No actionable findings in frozen Task014 source/tests or three scope documents. Independent focused C# checks passed46/46; reference Python oracle passed. Verdict applies only to dormant Task014 through retreat, not checkpoint G as a whole or host/public activation.

## Preliminary blind ledger — persisted before author/check packets

Target: branch `codex/combat-task008-reaction-lifecycle`, base/initial HEAD `6ee59808fc5b9ae565b1ed83cbf6da6a10e6b921`; six frozen source paths in `task014-source.sha256` all match. Three current documentation changes also reviewed. New helper/test files are untracked at initial inspection and included explicitly. Filename inventory exposed other artifact names only; no prior review, aggregate evidence, worker notes, rationale or CCE content consumed.

Canonical evidence: Task014 and checkpoint G in `docs/design/combat-cycle-implementation-plan.md`; Result2 specification, ordered schema, oracle transition; named source and tests, including unchanged replay/resolve boundaries and settlement validation.

- No actionable source defect identified in blind pass. Independent original-Config1 retreat opening ignores audit maximum; owner/shape/binding checks precede fallback; exact retry recovery precedes stale-version/clock admission. Stale timers cannot replace accepted disposition.
- Joint losses derive both roles from frozen paid participants. Refusal percentage, asymmetric rounding, capture conservation and threshold DP match bounded singleton/full10 contract. Pending captured lots retain pre-loss location. Retreat route derives from content graph and side anchor; actual movement updates representations, uses mandatory spending helper and adds victory RP only on completed evacuation.
- Hashed append methods re-enter validating private constructor; stage/identity/predecessor checks remain. Project reconstructs expected settlement and full allowed World; serialization compares complete typed equality before JSON patching. No cache introduced.
- Tests assert 144 literal events and 176 state hashes across 32 contexts, replay each prefix, retry each accepted command, discard candidate transitions, reject tampered receipts/world and future families. Intermediate state evidence is hashes, not retained literal JSON.
- Task013 diff only narrows future-family rejection to exclude newly authorized014; original result/cursor proof remains. Status stops at retreat; custody/relationships/closure/CA/ReserveRelease remain rejected.
- Remaining verification: execute retained Python oracle, focused C#014/013 and relevant shared World/spending checks after `/tmp/task014-after-boundary-20260920` exists; reconcile author/check claims. No .NET run before clearance. Core candidate discard is not host transaction durability. Actual positive campaign history, Snapshot/public activation remain open.

Preliminary conclusion: source/plan alignment supported within dormant bounded scope; final verdict withheld pending independent checks.

## Author-claim reconciliation

Author/check packets read only after preceding ledger persisted. Historical RED/GREEN and root/worker gate outcomes remain reported testimony, not reviewer-executed evidence.

| Claim | Independent evidence | Assessment |
| --- | --- | --- |
| Authenticated C3a/Round/Result; no supplied-state authority | `CampaignCombatResolution.Replay`, `ApplyTrustedBoundary`, codec `ReadState`; original Resolution tests | Confirmed by source; full predecessor replay and complete event/state byte comparison retained. |
| Independent Config1 opening and local clock; accepted intent stable | `Transition`, Result2 oracle, clock-matrix test | Confirmed by source; audit high-water never opens or gates a window. |
| Simultaneous loss/capture; pending origin; mandatory CP/DP and victory RP | `CampaignCombatLossRetreat`, settlement constructor, `CampaignCombatSpending` | Confirmed for selected full10/Cohesion0/one-hex profile. Cap implementation inspected; bounded traces cannot reach cap saturation. |
| Validated immutable append and full World equality | `ResultV2Next`, private constructor, `Project`, codec `WorldValue` | Confirmed; legacy constructor retained; arbitrary route/receipt or unrelated World changes cannot become replay authority. |
| 144 literal events /176 hash cuts,20 openings/56 choices | Independent fixture count; named seven new tests | Counts confirmed structurally; C# execution still pending clearance. These are cumulative014 counts, including013, not incremental additions or literal intermediate states. |
| Original013 proof retained;015/016 remain pending | Narrow cutoff diff; original32/64 test; new prefix final-status/future-family rejection | Confirmed by source. |
| Atomicity and remaining gates | Candidate discard tests, current docs, canonical plan | Core candidate immutability/replay only. No host durability, full positive campaign history, extended Snapshot12 or public activation claim accepted. |

## Verification in progress

- `python3 -B docs/specs/verify-combat-result-settlement-v2.py` — exit0:10 semantic groups;32 traces;304 reference cuts;3,728 mutations;1,360 raw rejects;384 timing checks;200 same-owner prior-time comparisons. Full reference oracle extends beyond C#014 and is not proof of later C# implementation.
- Read-only Python fixture inventory —32 contexts,144 literal prefix events,176 hash cuts; all required hashes present.
- `shasum -a 256 -c .planning/combat-task008-delivery/task014-source.sha256` — six matches initially and after root commit `e7c9e8f81b6ac5230dc1340ecbc54cbe5c0395ba`. Reviewer made no commit. Additional committed planning artifacts excluded from review content.
- Scoped `git diff --check 6ee5980 --` over tracked source/tests and three docs — exit0.
- Documentation SHA256: README `824abeb2f9577ed9f230c61998675304ceb7013614e632b69316495529b73f6c`; roadmap `49b5b726c0772a745dbde478e3d98b45d76b25a152a3c3f9b71771afe6a542b5`; tech-design `0c4712c23d5cd4a0788055c575fbcc50d49c3a7a1f23b76f0f63796839d11556`.

No .NET command run yet. Clearance marker remains absent at this update. Python process completed and closed.

## Plan review

Task014 requirements map directly to `CampaignCombatResolution.Transition` (intent, clocks, immutable event progression), `CampaignCombatLossRetreat` (simultaneous loss/capture and actual evacuation), `CampaignCombatObligations` (validated append chain), codec (causal readback and complete typed World check), and focused tests. Five material paths plus narrow sixth Task013 future-family cutoff are justified; no unrelated implementation change found. Original paid context, commitment, attack/target history and result RNG remain fixed through settlement.

Required conservation, refusal, zero-loss retreat, loss-DP threshold, mandatory CP10→11, incremental DP, actual-evacuation-only victory RP, pending-lot origin, ownership, malformed payload rejection, retries and every retained event cut have concrete evidence. Independent opening uses original Config1; local high-water and exclusive deadline govern choices. Acceptance before timer callback remains final. Hashed append paths all revalidate identity, stage, role and receipt predecessor.

Canonical dependency order remains correct:015 custody then016 relationships/closure; no ReserveRelease or public action activation here. Current README/roadmap/tech-design accurately describe implementation under verification and later gates. Canonical plan's historical progress entry still names014 next, consistent with root owning acceptance. No plan expansion needed. No naming/architecture/transport change warrants additional files.

## Final verification performed

Clearance file `/tmp/task014-after-boundary-20260920` observed before first .NET invocation. Its root-reported2318 full/81 Boundary passes are testimony; reviewer did not inspect aggregate logs or independently rerun those gates. Updated authorized checks packet likewise reports build/full format/CI progress; those reports are not substituted for independent checks below.

1. `dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --no-build --filter-class '*CombatLossRetreatTests' '*CombatResolutionTests' '*CombatWorldTests' '-bl:/tmp/task014-review1-{}.binlog'`
   - Native MTP/xUnit configuration verified from `global.json`, project and central properties. Invoked with `login:false` and approved IPC escalation, after clearance. Existing build reused as authorized; no build-server-disabling option.
   - Exit0;46 passed,0 failed,0 skipped;22.261s. Includes seven Task014 tests, ten retained Task013 tests and29 shared World tests.
   - Binlog exists: `/tmp/task014-review1-20260920-123649--35349--eOUHXx-dotnet-test.binlog` (655,629 bytes at inspection).
2. `python3 -B docs/specs/verify-combat-result-settlement-v2.py` — exit0; exact results recorded above. No fixture regeneration.
3. `shasum -a 256 -c .planning/combat-task008-delivery/task014-source.sha256` — all six match, including final verification at candidate `e7c9e8f81b6ac5230dc1340ecbc54cbe5c0395ba`.
4. `shasum -a 256 README.md docs/roadmap/pre-alpha-roadmap.md tech-design.md` — final values match recorded documentation hashes above.
5. `git diff --check 6ee5980 -- src/Cna.Core/Campaigns/CampaignCombatResolution.cs src/Cna.Core/Campaigns/CampaignCombatResolutionCodec.cs src/Cna.Core/Campaigns/CampaignCombatObligations.cs src/Cna.Core/Campaigns/CampaignCombatLossRetreat.cs tests/Cna.Core.Tests/Campaigns/CombatResolutionTests.cs tests/Cna.Core.Tests/Campaigns/CombatLossRetreatTests.cs README.md docs/roadmap/pre-alpha-roadmap.md tech-design.md` — exit0, no output.
6. Read-only Python fixture count independently derives32 contexts/144 prefix events/176 hash cuts. No artifacts written by that check.

Root committed identical pinned source during review. Final Git status showed only root's changed checks packet; reviewer wrote only this report, apart from explicitly required test-generated logs/artifacts. No source/Git mutations, CCE calls, agents or additional review instances. Both reviewer-launched Python and C# processes exited; no reviewer process remains running.

## Open questions and residual risks

No blocking question for bounded014. Synthetic trusted C3a evidence does not prove actual positive campaign history. Candidate discard/replay does not prove host transaction atomicity or durable publication. Later custody/closure, extended Snapshot12, HOST-PUB-001, coherent outward history and public admission remain separate gates. Victory saturation is unreachable from admitted Cohesion0 and one assault; cap implementation is inspected, not claimed as an exercised saturation case. Generalized multiunit/partial allocation, longer routes and other content profiles remain excluded by existing admission contract.

## Verdict

Ready.

## Recommended next actions

Return review instance1of3 to root for acceptance within existing budget. No fixes or extra review workstreams requested. Root owns remaining full-gate/CI reconciliation and permission to advance015.
