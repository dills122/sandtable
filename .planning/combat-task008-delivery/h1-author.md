# Author Explanation — H1 pre-cycle retained history

## Intent and plan
Implement first runtime H slice after accepted literal H0 contract: one independently retained
ordered event stream, trusted request and mandatory Created11. Derive causal family partitions
through atomic first opening. Currentbase4139675; fiveprimaryfiles h1-source.sha256. H2/H3 later
route Movement/Breakdown/Reaction; H4 owns literal fullroot codec and admission-disabled seam.
No public registration, host/persistence or later gameplay. No all28runtime/publication closure.

## Approach and components
CampaignCombatRetainedHistory checks count512, nonemptyCreated/event≤1MiB and aggregate16MiB before
payload copying/replay. Validated references retained once, then privately copied; exported buffers
are fresh copies. No missingCreated generation fallback. This is bounded capture, not authentication.
CampaignCombatHistoryReplay then invokes strict accepted causal readers in order. Four preamble,
oneWeather, fourstage gates are fixed profile partitions; count merely locates them, each complete
canonical event must validate against actual predecessor. At nineevents/state10, use actual Reserve
reader so membership exists. Optionaldesignation/completion stay distinct even at sameheadversion.
Accepted ReserveOpening reader rejects unsupported tails, including otherwisevalid later Move4.
CampaignCombatHistoryProjection is closedtyped union of actual accepted family states with shared
version/prefix/position/receipt access. Result carries owned transcript plusprojection; neither is
an independently admissible snapshot/cache. No opaque JSON persistedstate or newwirecontract.

## Verification and traceability
Tests cover17seeds,bothorderowners,selectedReserveforks/everyprefix. Cumulativeidentitycheck equals
all256H0precyclevectors/208distincthistories; perprefix tests replay actualrouter and compareH0
creation/eventhashes,headers,World,RNG,receipts,positions and availablecycleauthority. Four variants
also match predecessor exactlength/hash goldens. Fullroot literal parity deliberately notclaimed.
Malformed/missing/trustedcreation/request,internal omission/duplication/reorder/wrongsuccessor,
canonical re-signedWeatherRNG,validunsupportedfuturetail,bounds andsource/resultbuffer mutation
negatives pass. Legalshorterprefix is valid; archive committedhead/truncation authentication is not
known from this API and remains laterroot/hosting contract. Details in h1-checks.md.
Finalfocused53pass,scopedformatpass. Rootdevreviewandbuild0warnings pass; fullgate running atauthor
freeze. Freshindependentreviews determine readiness, not this explanation.

## Choices, costs and limits
Reuse existingstrict readers rather than general handlerregistry or rewriting familylogic. This
replays some prefixes repeatedly and copies bounded buffers; selectedhistories are small, no broad
performance/campaigncapacityclaim. Closed union adds a projectioncase per laterfamily as needed.
Fixedarity precycle assumptions are existingcontractconstraints; unsupportedprofiles reject rather
than inventingdefaultstate. Private capture does not make arbitrary bytes trusted; only replay does.
No accepted predecessor code/schema/fixture changed; csproj onlylinks frozenH0testfixture.
Challenge count-vs-causality, exactReserveboundary, bufferownership, exhaustiveprefixinventory,
true re-signed negatives, no shortcut from localprojection to fullSnapshot/admission proof.
