# F2 participant lifecycle research

Read-only future scope; must wait F1 acceptance. Canonical inherited-reaction-lifecycle-v1 spec/schema
and frozen oracle. Exactly F1 actual two-owner trigger; four events: reacting move3 assault→ownrear,
participant completion3, mandatory System empty stop resolution2, System no-eligible window closure3.
Two traces/eight events/ten cuts/28 artifacts. Source state13; terminal17. Move alone adds progress.
World terminal phasingCP4 assault, reactorCP2 rear; original phasing route resumes byte-for-byte.

Likely five files new models/projector/codec/tests+fixture link, subject to F1 reusable model shape.
Do not fake Snapshot11 to use old factories; they require Content6/materialized positions. Existing
CampaignReactingElementMovedV2Factory and CampaignBreakdownLifecycleFactory supply pure logic evidence.
Reuse CampaignBreakdownRoute.Create/AtLocation, Stop.Create, Reacting/ReactorStopOpen/Moving flows,
existing stop/flow writers, CampaignObservationV6DisclosureIdentity capability/opportunity methods,
and candidate Action types where their input types match frozencost. Windowcapability helper is tied
Snapshot11; pure DisclosureIdentity.CreateWindow also rejects nonhistoricalV8 hashes, so cannot
reuse for Rules10 unchanged. Port exact pure ordered hash without synthetic snapshot or weakening
old guard. CapabilityKey/CreateOpportunity are not rules-hash gated and can be reused. New window authority must support
resolved set and active ID while retaining actual F1 trigger/suspended symbolic Movement.

Public command window differs persisted window; public opportunity changes by currentversion AND full
sorted move-option inventory. Even when completing after first move, rear→supply remains legal and
must be included in capabilityhash. Do not omit because selected trace chooses completion. Compare
old factory eligibility/controlled/occupied filters against frozen selected Content7/current typedWorld.
Only first move admitted by F2 but capability inventory remains actual. Distinct public/persisted IDs
must be explicit tests. Completion selects opportunity then resolves it, opens ReactionCompletedstop;
System resolve consumes reactorroute, leavesinactivewindow; noeligibleclose only after no active and
no legal unresolved opportunities. Close actor null in event but input System. Preserve exact phasing
route and current suspendedposition; no BreakdownDetermination or Combat transition.

Sources: new move mobility/cost exact; resolution21.24–21.26 existing iml receipt domain;
move/complete/close irl event-specific domains. Prefixactualfullbytes. Fullhistoryreplay, 0–4events,
1MiB/depth32/512bounds, exactauthorizedretryafterterminal; missing/reordered/duplicate/fifth rejection.
Tests bothowners allcuts/goldens, fulloption/handle rotation/authorityID substitution, wrongactor/cycle,
context/window/route/World/RNG/progress coherentforgeries, buffers, legacyreader and unsupportedsecondmove.
Initial H retains everycut including firstparticipantmove used byF4/F5. F2 alone doesnotcloseF.
Baseline passed exit0 /tmp/f2-reaction-baseline.log:2traces8events10cuts8retries754mutations72raw76boundaries16pins.

## Frozen F1 interface consequence
F1 window deliberately has immutable empty resolved IDs/null active ID; WriteWindow hardcodes that
closed profile. F2 can wrap actual F1 window with separate lifecycle active/resolved fields and own
writer, preserving five primary files without altering F1 source. Retain F1 typed trigger/position/
authority/opportunity and actual predecessor. F1 World writer guards only trigger World; F2 needs
its own typed postparticipant World equality guard. Do not feed forged F1 state to that writer or
relax its profile. Fullhistory state wrappers are internal; serialized bytes remain sole external
admission, with replay-derived canonical comparison. This is prospective design, not implemented.
