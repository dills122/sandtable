# Task017A1 preliminary review

Review instance: 2 of 3. Blind ledger recorded before author packet.

Frozen candidate 77ef166f03674a7e5875acb8c1ebfde36b0d406a; base afdcd9e. Independent detached clone `/tmp/sandtable-task017a1-review2-1925`. No prior review, memory, CCE, worker/check/dev testimony consulted.

Inspected canonical Task017 child plan, Release specification/schema/oracle, all four source/test changes, relevant immutable unit/cycle/RNG/attack values and fixture reconstruction. Actual diff includes thirteen metadata/documentation paths beyond four source/test paths; bootstrap explicitly excludes ongoing metadata and limits implementation review to four paths. No generated or unrelated runtime changes.

No actionable defect established in blind pass. Base read compares complete canonical bytes against independently retained expected object; does not claim candidate authentication. Collections copied and read-only, nested records immutable. Raw parser independently enforces inventory/order, bounds, rational shape and hash types before expected context. Member ordering, side/creation binding, scope, first/later history, prior release and expired exception match frozen validate_base rules. Unreferenced attack records remain structurally checked only, consistent with isolated contract; linked commitments require unique matching identity and scope. Native 44-row reconstruction uses recipes and independent retained creation values, not expected base hashes as inputs; four settled rows explicitly excluded.

Concerns to resolve: own C# test execution pending because initial sandbox restore failed NU1900 against NuGet; retry with network/IPC approved. Frozen oracle independently passed 13 cases/48 traces, 188 cuts,2368 mutations,840 raw rejects,20 timing,27 boundaries; these are Python contract results only. Need boundary tests and final claim reconciliation. Full solution gate outside this pass, reserved for later independent review.
