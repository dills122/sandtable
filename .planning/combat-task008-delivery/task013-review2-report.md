# Task013 independent review

Review instance: 2 of 3. Reviewer mode; acceptance and budget remain with root.

## Findings

No actionable findings in frozen Task013 source or scoped documentation. No fixes requested. Blind ledger below was written before author/checks packets; final assessment follows it.

## Blind preliminary ledger — persisted before author/checks packets

Target: branch `codex/combat-task008-reaction-lifecycle`, base `f2c0046`, observed HEAD `1c514640b38e43abf72f1e9725ec8d7e7e8a8e8c`. All six entries in `task013-source.sha256` verified OK. Current README, roadmap and tech-design changes inspected. Git status initially showed only execution.md and task013-checks.md modified; their contents were not read. Source changes already committed; frozen bytes match. No CCE calls, prior reviews, aggregate evidence, dev/worker notes, proposals or execution history consumed. Exact local reads and scoped searches only; no agents or source/Git mutation.

Blind evidence: canonical Task013/checkpoint F/G; Result2 spec, ordered schema and relevant oracle functions; all new resolution production/test code; narrow identity/obligations diffs; existing selected-rule implementation and test inventory; RNG implementation; test project/platform configuration.

No actionable defect established in blind pass. Items for executable verification and claim reconciliation:

- 32 literal resolve event comparisons and 64 initial/resolved state HASH comparisons, with authenticated paid Round2 bytes first. No claim of literal intermediate state JSON.
- Corpus asserts 16 eight-draw, 16 nine-draw, 12 rejection traces, zero block crossings. Separate cursor30 vector reconstructs C3a and Round2 before crossing into byte38; overflow probes MaxValue and MaxValue−3.
- Resolution uses existing full selected-rule implementation. Full-table correctness needs shared rules checks; 32 Result2 traces alone are not exhaustive table enumeration.
- One immutable candidate contains result, RNG cursor, pending typed World, version/prefix and receipt. Discard/retry proofs are Core-only; no host transaction/process-restart proof.
- Readback authenticates separate predecessor/round/result inputs and compares complete regenerated bytes; embedded event.input does not establish trust. Original Config1 and supplemental round clock identities remain distinct.
- Factory only relaxes legacy synthetic occurrence-ID relationship via named internal entry; derives settlement ID, keeps paid-state/original-role checks and all consequence receipts null. Default constructor retains old checks.
- Result syntax profile propagates only explicit Route permission; C3a rejects Route and both profiles reject nonempty LegacyBrokenVehicleLot. Primitive parsing, shape/canonical checks and size/depth/array checks precede trusted history access.
- World serialization checks typed World against paid World plus derived singleton pending settlement before using retained canonical bytes; defensive copies protect event bytes, draws, consumed bytes and receipts.
- Plan boundaries respected: no disposition, loss, custody, closure, Reserve advance, HistoryReplay/Snapshot or public/host activation. Five material paths plus mechanical fixture link fit bounded implementation despite original 3–5-file estimate.

Next: read only designated author/checks packets, run proportionate focused/shared native MTP checks using cleared existing build, reconcile claims and finalize verdict. Preliminary ledger is not acceptance.

## Code And Plan Review

`CampaignCombatResolution.cs:115` rebuilds accepted012 through existing trusted Boundary/C3a/Round replay, requires paid committed state with both seals and five step receipts, then independently replays Result2 inputs. Owned event bytes must equal regenerated whole events. No arbitrary caller-supplied committed cache or embedded event.input self-authentication enters this path.

`CampaignCombatResolution.cs:151` validates shape, binding, actor and field combinations before retry lookup. Exact retained commands recover original event bytes despite changed valid clock metadata; fresh resolve requires expected version, no decision ID, null time and available confidence. Stale timers are no-ops. Later settlement work is rejected. Candidate creation is local and immutable; failure or discard cannot advance retained cursor. Overflow probes cover failure before first byte and after partial draws.

`CampaignCombatResolution.cs:197` consumes existing selected procedure's eight ordered purposes, retains rejection bytes and conditionally draws only captured role's ninth die. Existing selected adjudicator owns tables and arithmetic. Full-table shared test checks all 1,296 Morale pairs and 6,480 joint assault combinations (8,840 selected result/refusal/capture rows). Result2 integration corpus independently fixes 32 resolve events and 64 initial/resolved hashes; it does not enumerate every RNG/table combination. Separate authenticated cursor30 case supplies absent corpus block-crossing coverage.

`CampaignCombatResolutionCodec.cs:102` checks typed World equality against authenticated paid World plus derived pending settlement. `CampaignWorldV7.cs:157` equality covers all World collections and binding. Canonical paid bytes therefore cannot silently normalize away a typed mutation. Both pre-loss paid elements, resources, positions, representations and existing facts remain unchanged; only singleton settlement appears, with five consequence receipts null.

`CampaignCombatObligations.cs:241` exposes narrow internal resolved-only factory, derives set identity from exact lowercase cmt/res hashes, and preserves original opposing participants, scope, paid CP/ammunition, Cohesion0 and TOE checks. Public legacy constructor still demands synthetic `.commit`/`.result` relationship. `CampaignCombatIdentityCodec.cs:235` opts only Result2 into Route grammar; C3a remains restrictive and LegacyBrokenVehicleLot stays forbidden. Result2 owns closed local shapes and inherited Timing/Receipt declarations; canonical comparison rejects alternative ordering, escaping and whitespace. Integer, UInt64, UTC, shape, array/depth and byte checks precede trusted context.

Task013/checkpoint F acceptance is met for dormant Core resolution. Shared full-table tests supplement bounded Result2 vectors. Checkpoint G work remains deferred as planned: disposition, losses, actual retreat, custody, relationships, closure and Reserve progression. Original 3–5-file estimate expands to five material paths plus one mechanical fixture link, without unrelated production changes. README/roadmap correctly describe verification underway; tech-design explicitly separates Core candidate atomicity from host durability and actual positive campaign history. No activation, migration or rollout added; no need to invent such scope for this internal mechanism.

## Author-Claim Reconciliation

Read `task013-author.md` and `task013-checks.md` only after preliminary ledger persisted.

| Claim | Evidence | Status / consequence |
| --- | --- | --- |
| Separate trusted inputs authenticate paid Round2; owned reconstructed context | Replay at Resolution:115; inherited SealedRound replay; forgery/ownership tests | Confirmed; no caller cache authority |
| One candidate carries result/cursor/pending World/event/head, no consequences | Transition:151, PendingWorld:193, codec WorldValue:102; focused tests | Confirmed for Core; not host transaction evidence |
| 32 literal events and 64 hash cuts | Focused literal test passed; fixture cardinality audit | Confirmed; intermediate state evidence is hashes, not literal JSON |
| 16 eight-draw / 16 nine-draw / 12 rejection / zero corpus crossings, separate cursor30 crossing | Focused draw/crossing tests; independent fixture audit | Confirmed; supplemental test rebuilds predecessor lineage, not cursor patch on paid cache |
| Full selected outcomes | Existing adjudicator reused; shared CombatSelectedRulesTests passed | Confirmed as shared table proof plus bounded integration, not exhaustive Result2 table vectors |
| Resolved-only factory preserves legacy checks; named grammar preserves C3a | Narrow diffs and focused/shared tests | Confirmed; Route syntax admission does not authorize later transitions |
| Raw grammar, causal readback, UInt64 bounds and ownership | Focused sentry, forged/rehashed event/state, collection-copy and overflow tests | Confirmed within tested boundaries |
| 18 local descriptors | Independent audit matches all 16 Result2 schema objects; two additional local shapes are inherited Timing and Receipt | Reconciled; no contradictory count |
| Root full build/suite/format/Boundary/oracle pass; CI status | Designated checks packet reports these results | Reported, not rerun or independently certified here; no CI acceptance claim |
| No host publication, actual positive campaign provenance, extended snapshot/public activation | Production scope, plan and tech-design | Confirmed exclusions; remain root-owned gates |

## Verification Performed

All shell invocations used `login:false`. No CCE tool/server/process invoked. No agents, fixes, commits or extra review instances. Only own report deliberately edited; test outputs/binlogs written under `/tmp` with authorized native MTP IPC. Existing build used after explicit clearance. SDK observed `10.0.400`; global.json native MTP, SDK-style net10.0 executable and xunit.v3.mtp-v2 verified. No `--disable-build-servers` passed to dotnet test.

1. `shasum -a 256 -c .planning/combat-task008-delivery/task013-source.sha256` — all six OK before review and after checks. HEAD remained `1c514640b38e43abf72f1e9725ec8d7e7e8a8e8c`; branch/base verified. Scoped `git diff --check f2c0046 HEAD --` over six pinned paths plus README/roadmap/tech-design passed, exit0.

2. Focused command — exit0, 10 passed / 0 failed / 0 skipped, 12.265s:

```sh
dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --no-build --filter-class 'Cna.Core.Tests.Campaigns.CombatResolutionTests' --results-directory /tmp/task013-review2-focused-results '-bl:/tmp/task013-review2-focused-{}.binlog'
```

Retained binlog: `/tmp/task013-review2-focused-20260920-120029--32696--QFaKKb-dotnet-test.binlog`.

3. Shared command — exit0, 143 passed / 0 failed / 0 skipped, 17.580s:

```sh
dotnet test --project tests/Cna.Core.Tests/Cna.Core.Tests.csproj --no-build --filter-class 'Cna.Core.Tests.Campaigns.CombatIdentityTests' 'Cna.Core.Tests.Campaigns.CombatWorldTests' 'Cna.Core.Tests.Campaigns.CombatSealsTests' 'Cna.Core.Tests.Campaigns.CombatCommitTests' 'Cna.Core.Tests.Campaigns.CombatStepsTests' 'Cna.Core.Tests.Rules.CombatSelectedRulesTests' 'Cna.Core.Tests.Rules.CombatRulesInputArtifactTests' --results-directory /tmp/task013-review2-shared-results '-bl:/tmp/task013-review2-shared-{}.binlog'
```

Retained binlog: `/tmp/task013-review2-shared-20260920-120103--32722--v8QZt8-dotnet-test.binlog`.

4. Read-only `python3 -B -` audit — exit0: recomputed every fixture `sourceHashes` entry against exact docs/specs-relative file bytes (11 matched); extracted codec shape strings and compared all 16 local schema objects; counted 32 traces, 16 eight-draw, 16 nine-draw, 12 rejection traces and zero corpus crossings; required at least initial/resolved hash cuts; compared built Result2 fixture byte-for-byte with canonical fixture. No fixture regeneration or Python bytecode writes.

5. Both native test sessions completed exit0. Final approved `ps -axo pid,ppid,comm | rg 'dotnet|Cna.Core.Tests|MSBuild|VBCSCompiler' || true` returned no processes. Initial sandbox process listing was denied by OS; approved final listing succeeded. Routine missing-path probes found no docs/schemas, project-local config.toml or CampaignWorldSnapshotV7.cs; canonical specs/config and actual CampaignWorldV7.cs read instead. No verification failure remained.

## Open Questions And Residual Risks

No unresolved question blocks scoped review. Existing compiled build trusted per supplied clearance; no new build, full suite, full format, Boundary gate, remote CI or complete Python oracle rerun in this instance. Root-reported full-suite and CI evidence remains root-owned. Existing full Result2 Python oracle covers later settlement stages and must not be counted as C# Task013 implementation coverage. Synthetic C3a lineage, in-memory discard/replay and deterministic retry do not prove host transactions, real process crashes, durable publication, creation-rooted positive play or public information-flow safety.

## Verdict

Ready — frozen Task013 dormant Core result/cursor scope and scoped docs only. Root owns delivery acceptance and remaining gates.

## Recommended Next Actions

Return this report to root for acceptance and reconciliation. No code correction requested; no additional review/workstream initiated. Review processes closed; six source pins unchanged.
